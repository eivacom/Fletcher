# SPDX-License-Identifier: LGPL-3.0-or-later
# Copyright (C) 2026 The Fletcher Authors
#
name: ci.core

# Group is keyed on `github.ref`, so two PR commits to the same ref
# cancel each other (cancel-in-progress: true) while tag pushes
# (different ref per tag) and parallel PRs (different ref per PR) stay
# independent. Hardcoded workflow name avoids the `github.workflow`-
# resolves-to-caller trap in workflow_call context.
concurrency:
  group: ci.core-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_call:
    inputs:
      devcontainer-image:
        description: >-
          Pre-built devcontainer image reference (e.g.
          eivaorg/fletcher-devcontainer:<sha>). The caller is
          responsible for invoking ci.setup-devcontainer-image.yml; this
          workflow only consumes the resulting image. ci.pr.yml passes the
          image it built once and shares across all callers; the
          cd.core.yml wrapper does the same for tag-push releases.
        required: true
        type: string
    outputs:
      conan-ref:
        description: >-
          Conan package reference (name/version) computed on tag-push by
          build-windows. Empty on PR runs. Consumed by cd.core.yml's
          upload job to verify the tag matches conanfile version.
        value: ${{ jobs.build-windows.outputs.conan-ref }}

jobs:
  build-windows:
    runs-on: windows-2022
    outputs:
      conan-ref: ${{ steps.get-ref.outputs.conan-ref }}
    steps:
      - uses: actions/checkout@v6
        with:
          sparse-checkout: |
            core
            .github
            .conan-profiles

      - name: Install Conan
        uses: ./.github/actions/conan-install

      - name: Build and test
        shell: bash
        working-directory: core
        run: conan create . --build=missing -pr:a=../.conan-profiles/Windows-msvc194-x86_64-Release -o "&:run_tests=True"

      - name: Get Conan reference
        id: get-ref
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        shell: bash
        working-directory: core
        run: |
          ref=$(conan inspect . --format json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['name'] + '/' + d['version'])")
          echo "conan-ref=$ref" >> "$GITHUB_OUTPUT"

      - name: Save Conan package
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: conan cache save "${{ steps.get-ref.outputs.conan-ref }}:*" --file "fletcher-core-windows-conan-package.tgz"

      - name: Upload Windows Conan package .tgz artifact
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v7.0.1
        with:
          name: fletcher-core-windows
          path: fletcher-core-windows-conan-package.tgz
          retention-days: 5

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          sparse-checkout: |
            core
            .github
            .conan-profiles

      - name: Pull devcontainer image
        uses: ./.github/actions/pull-devcontainer-image
        with:
          image: ${{ inputs.devcontainer-image }}
          dockerhub-token: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and test
        shell: bash
        env:
          ARTIFACT_NAME: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && 'fletcher-core-linux-conan-package.tgz' || '' }}
        run: |
          docker run --rm \
            -v "${GITHUB_WORKSPACE}:/workspace" \
            -w /workspace/core \
            -e ARTIFACT_NAME="$ARTIFACT_NAME" \
            fletcher-build \
            bash -c '
              set -e
              conan create . --build=missing -pr:a=/workspace/.conan-profiles/Linux-gcc13-x86_64-Release -o "&:run_tests=True"
              if [[ -n "$ARTIFACT_NAME" ]]; then
                REF=$(conan inspect . --format json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d[\"name\"]+\"/\"+d[\"version\"])")
                conan cache save "$REF:*" --file "/workspace/$ARTIFACT_NAME"
              fi
            '

      - name: Upload Linux Conan package .tgz artifact
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v7.0.1
        with:
          name: fletcher-core-linux
          path: fletcher-core-linux-conan-package.tgz
          retention-days: 5
