# SPDX-License-Identifier: LGPL-3.0-or-later
# Copyright (C) 2026 The Fletcher Authors
#
# Release orchestration for fletcher-fastdds-pubsub-provider. Owns the setup-devcontainer
# step + invokes fletcher-fastdds-pubsub-provider.yml with the resulting image. The actual
# build/test/upload logic lives in fletcher-fastdds-pubsub-provider.yml so PR runs (via
# pr.yml) and release tag-push runs share the same source of truth.
#
# Triggers:
#   * push: tags 'fastdds-pubsub-provider-v*'     — real release; fletcher-fastdds-pubsub-provider's upload
#                                 job runs (it gates on event_name == 'push').

name: release-fastdds-pubsub-provider

on:
  push:
    tags:
      - 'fastdds-pubsub-provider-v[0-9]*.[0-9]*.[0-9]*'

jobs:
  setup-devcontainer:
    uses: ./.github/workflows/setup-devcontainer-image.yml
    secrets: inherit

  release:
    needs: setup-devcontainer
    uses: ./.github/workflows/fletcher-fastdds-pubsub-provider.yml
    with:
      devcontainer-image: ${{ needs.setup-devcontainer.outputs.image }}
    permissions:
      contents: write
    secrets: inherit
