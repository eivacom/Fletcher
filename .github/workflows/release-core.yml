# SPDX-License-Identifier: LGPL-3.0-or-later
# Copyright (C) 2026 The Fletcher Authors
#
# Release orchestration for fletcher-core. Owns the setup-devcontainer
# step + invokes fletcher-core.yml with the resulting image. The actual
# build/test/upload logic lives in fletcher-core.yml so PR runs (via
# pr.yml) and release tag-push runs share the same source of truth.
#
# Triggers:
#   * push: tags 'core-v*'     — real release; fletcher-core's upload
#                                 job runs (it gates on event_name == 'push').

name: release-core

on:
  push:
    tags:
      - 'core-v[0-9]*.[0-9]*.[0-9]*'

jobs:
  setup-devcontainer:
    uses: ./.github/workflows/setup-devcontainer-image.yml
    secrets: inherit

  release:
    needs: setup-devcontainer
    uses: ./.github/workflows/fletcher-core.yml
    with:
      devcontainer-image: ${{ needs.setup-devcontainer.outputs.image }}
    permissions:
      contents: write
    secrets: inherit
