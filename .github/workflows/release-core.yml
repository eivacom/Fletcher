# SPDX-License-Identifier: LGPL-3.0-or-later
# Copyright (C) 2026 The Fletcher Authors
#
# Release orchestration for fletcher-core. Owns the setup-devcontainer
# step, invokes fletcher-core.yml with the resulting image to build and
# upload artifacts, and creates the GitHub Release. fletcher-core.yml is
# also called from pr.yml — build/test logic is shared so PR and release
# runs use the same source of truth. The upload job lives here so PR
# runs do not show a skipped upload rectangle.
#
# Triggers:
#   * push: tags 'core-v*'     — real release.

name: release-core

on:
  push:
    tags:
      - 'core-v[0-9]*.[0-9]*.[0-9]*'

jobs:
  setup-devcontainer:
    uses: ./.github/workflows/setup-devcontainer-image.yml
    secrets: inherit

  release:
    needs: setup-devcontainer
    uses: ./.github/workflows/fletcher-core.yml
    with:
      devcontainer-image: ${{ needs.setup-devcontainer.outputs.image }}
    secrets: inherit

  upload:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          sparse-checkout: |
            core
            .github

      - name: Verify tag version matches conanfile.py version
        uses: ./.github/actions/verify-tag-version
        with:
          conan-ref: ${{ needs.release.outputs.conan-ref }}
          tag-prefix: core-v

      - name: Download artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          pattern: fletcher-core-*
          merge-multiple: true

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            "fletcher-core-windows.tgz" \
            "fletcher-core-linux.tgz"
