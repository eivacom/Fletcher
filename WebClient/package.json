{
  "name": "@fletcher/web-client",
  "version": "0.1.0",
  "description": "TypeScript client for the Fletcher WebGateway — WASM-accelerated row decoding with Arrow JS and plain-object backends",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "wasm/build/fletcher_decoder.wasm",
    "wasm/build/fletcher_decoder.js"
  ],
  "scripts": {
    "build:wasm": "cd wasm && emcmake cmake -S . -B build && cmake --build build",
    "build:ts": "tsc",
    "build": "npm run build:wasm && npm run build:ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "apache-arrow": ">=15.0.0"
  },
  "peerDependenciesMeta": {
    "apache-arrow": {
      "optional": true
    }
  }
}
