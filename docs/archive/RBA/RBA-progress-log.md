# RBA — Progress Log

One section is appended per item by the runbook after it is green, reviewed,
logged, and pushed. See
[RBA-recordbatch-accessor.md](RBA-recordbatch-accessor.md) for the tracker.

<!-- Entries appended below by the round runbook -->

## RBA-1 — Option plumbing + additive no-drift guarantee (2026-06-24)

**Forcing test:** `AccessorTest.OptGatedEmissionLeavesExistingOutputsByteIdentical` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-1-opt-plumbing-no-drift.md`  ·  Step-2: APPROVE (after 1 rework cycle — 4 items resolved)
**What landed:** Two default-off `--fletcher_opt` tokens (`accessor`, `rust`) that each emit one new output file (`<stem>.fletcher.accessor.pb.h`, `<stem>.fletcher.rs`) via **new** emitter entry points, unconditionally (one file per token, never content-gated). No existing emitter function touched and no existing output byte changed (D-RBA-1). RBA-1 files are minimal-but-valid skeletons; real accessor content lands RBA-2+.
**Files touched:** `protoc/src/generator.cpp` (opt parse + 2 additive `context->Open` blocks + 2 filename helpers), `protoc/src/recordbatch_accessor_emitter.{hpp,cpp}` (new), `protoc/CMakeLists.txt`, `integration-tests/protoc-arrow-bridge/CMakeLists.txt` (new `accessor_tests` target, 3 `-I` roots), `integration-tests/protoc-arrow-bridge/tests/test_accessor.cpp` (new), `integration-tests/protoc-arrow-bridge/proto/empty_accessor.proto` (new degenerate fixture).
**Reviews:** compliance `CONFORMANT` (initial HIGH on Rust well-formedness silent downgrade → fixed) · code-review `blocking 0 (2 resolved: staging + rustc-proof) / should-fix 0 (majors adjudicated) / nits logged`  (full: `plans/reviews/RBA-1-conformance.md`, `plans/reviews/RBA-1-codereview.md`)
**Verification:** inner-loop `PASS` — plugin unit suite 3/3; integration `ctest` 64/64 (62 pre-existing + 2 new Accessor); `rustc` parse-check RAN on this box (rustc 1.96.0, not skipped). Full round-wide suite (core unit tests + Rust crate) deferred to a later stage-close — the Rust crate does not exist until RBA-5. (accepted residuals: none)
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor`
**Carry-forwards / stop-and-asks:** none. (Won't-fix nits logged: unknown-token errors out of scope — acceptance = unknown ⇒ no new files; `FieldDescriptor*` null guards unneeded in plugin context; docs deferred to RBA-7.)

## RBA-2 — C++ scalar accessor + positional type-check validation (2026-06-24)

**Forcing test:** `AccessorTest.ScalarColumnsReadAndValidatePositionally` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-2-cpp-scalar-accessor.md`  ·  Step-2: APPROVE (after 1 rework cycle — 3 items; reviewer also fixed inline the anon-ns→`namespace fletcher` linkage requirement)
**What landed:** First real generated C++ `<Class>Accessor`: dual `Make(RecordBatch)`/`Make(StructArray)` factories sharing one `FromColumns_` (column-count gate + per-column `type()->Equals(*expected,/*check_metadata=*/false)` + `null_count()==0` for proto-non-nullable), caching `std::shared_ptr<ConcreteArray>` columns (buffers owned — no source ref), scalar getters (`Value`/zero-copy `GetView`; nullable → `std::optional`/`std::string_view`), `num_rows()`. Validation is positional + type-only (name- and nullable-flag-tolerant). Composite-containing messages **fail fast** (RBA-4 will add real handling). Metadata getters deferred to RBA-3 (storage laid). Non-behavioural helper relocation: field-walk helpers moved anon-ns → `namespace fletcher` via new shared internal header `protoc/include/generator_internal.hpp`.
**Files touched:** `protoc/src/recordbatch_accessor_emitter.cpp`, `protoc/include/generator_internal.hpp` (new), `protoc/src/generator.cpp` (linkage move + include), `integration-tests/protoc-arrow-bridge/{CMakeLists.txt, proto/accessor_scalar.proto (new), tests/test_accessor_scalar.cpp (new)}`.
**Reviews:** compliance `CONFORMANT` (initial blocking: composite-validation gap → fixed via fail-fast) · code-review `blocking 0 (3 "missing files" = untracked/staging; 2 CRITICAL/MAJOR confirmed FALSE POSITIVES with evidence — Arrow `field(i)` pre-windows; `Status::Invalid` is variadic) / minors addressed (test strengthened)`  (full: `plans/reviews/RBA-2-conformance.md`, `plans/reviews/RBA-2-codereview.md`)
**Verification:** inner-loop `PASS` — integration `ctest` 67/67 (Accessor suite 5/5); RBA-1 no-drift `OptGatedEmissionLeavesExistingOutputsByteIdentical` 🟢 (proves the helper relocation changed no emitted bytes). Full round-wide suite deferred to a later stage-close (Rust crate not built until RBA-5). (accepted residuals: none)
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor`
**Carry-forwards / stop-and-asks:** **[RBA-4/RBA-5/6]** cross-language `StructArray` child-windowing asymmetry — Arrow **C++** `StructArray::field(i)` returns children already sliced to `[offset,len)` (do NOT re-`Slice`), whereas arrow-rs `StructArray::columns()` is **not** pre-windowed (Rust `from_struct` MUST slice). Design doc RBA-2 corrected for the record; RBA-4 (C++ nested) and RBA-5/6 (Rust) must honor the per-language rule.

## RBA-3 — C++ generic metadata access (2026-06-24)

**Forcing test:** `AccessorTest.ExposesSchemaAndFieldMetadataGenerically` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-3-cpp-generic-metadata.md`  ·  Step-2: APPROVE (cycle 1; 2 non-blocking doc-only clarifications folded in)
**What landed:** Three public getters on every generated `<Class>Accessor`, returning `const arrow::KeyValueMetadata*` over RBA-2's already-stored `schema_metadata_`/`fields_`: `schema_metadata()`, `field_metadata(int i)` (canonical; null on OOB / null field / absent), `field_metadata(const std::string& name)` (linear live-name scan; null on unknown). Metadata is exposed **verbatim and domain-agnostic** — the generator references no keys (D-RBA-5). Absent/OOB/unknown → `nullptr`, never throw. Pure add-on (D-RBA-1). Emitted comments preserve the `Field::metadata()` by-value-copy/co-ownership lifetime justification + the `schema_metadata()` null contract (struct source / no-metadata batch).
**Files touched:** `protoc/src/recordbatch_accessor_emitter.cpp` (3 getters + `<string>` include), `integration-tests/protoc-arrow-bridge/tests/test_accessor_scalar.cpp` (new `ExposesSchemaAndFieldMetadataGenerically`).
**Reviews:** compliance `PASS` (initial blocking: test used domain-flavored key `unit=m/s` → neutralized to arbitrary `field_key_0=field_val_0` / `schema_key_0=schema_val_0`; emitter was always generic) · code-review `blocking 0 / major 0 / minor 0 / nit 0` (clean — bounds/null/lifetime/name-scan all verified)  (full: `plans/reviews/RBA-3-conformance.md`, `plans/reviews/RBA-3-codereview.md`)
**Verification:** inner-loop `PASS` — Accessor suite 6/6; integration `ctest` 68/68; RBA-1 no-drift + RBA-2 scalar still 🟢. Full round-wide suite deferred to a later stage-close (Rust crate not built until RBA-5). (accepted residuals: none)
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor`
**Carry-forwards / stop-and-asks:** **[RBA-6]** metadata-representation parity — C++ returns `nullptr` for absent metadata, the Rust side returns an empty `HashMap`; the cross-language parity check (RBA-6/RBA-7) must treat C++-`nullptr` ≡ Rust-empty-map, not a literal mismatch.

## RBA-4 — C++ full type parity (struct / list / map / nested) (2026-06-24)

**Forcing test:** `AccessorTest.CompositeColumnsReadColumnOriented` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-4-cpp-composite-parity.md`  ·  Step-2: APPROVE (after 1 rework cycle — 5 items; RBA-4a/RBA-4b split ENDORSED)
**What landed:** Full C++ composite parity, implemented in two test-first passes under one design:
- **RBA-4a** (commit `985165e`): STRUCT (incl. ≥2-level + cross-file), REPEATED_SCALAR, REPEATED_STRUCT; `RowView`+`at(row)`+`is_null(row)` on every accessor (D-RBA-7); new borrowed-span header `arrow-bridge/.../recordbatch_spans.hpp` (`ScalarSpan`/`StructSpan`); composite recursion via Arrow C++ `StructArray::field(i)` directly (no re-Slice; coordinate-consistent with the retained whole-StructArray validity); cross-file accessor `#include`s reuse the canonical `CollectCrossFileIncludes` read-only (relocated to `namespace fletcher` via `generator_internal.hpp` — pure linkage move) with `.fletcher.pb.h`→`.fletcher.accessor.pb.h` mapping (D-RBA-10). Row-nullable collections → `std::optional<Span>` (None on null list row, B2).
- **RBA-4b** (this commit): MAP scalar-value (`ScalarMapSpan`) + message-value (`StructMapSpan`); NESTED_LIST depth-2 **and** depth-3 (`NestedStructSpan`); struct map-values / list elements → `std::optional<RowView>` (None on null, B2); all composite children cached as explicit `std::shared_ptr<…>` handles per design (D-RBA-7 explicit-handle ownership).
**Files touched:** `protoc/src/recordbatch_accessor_emitter.cpp`, `protoc/src/generator.cpp` (+`generator_internal.hpp`, linkage move), `arrow-bridge/include/fletcher/arrow_bridge/recordbatch_spans.hpp` (new), `integration-tests/protoc-arrow-bridge/{CMakeLists.txt, proto/accessor_geo.proto (new), proto/accessor_composite.proto (new), tests/test_accessor_composite.cpp (new)}`.
**Reviews:** RBA-4a — compliance `PASS` (initial blocking: sibling cross-file collector fork → replaced with canonical-collector reuse) · code-review `clean` (initial major: row-null lists erased → optional<Span>). RBA-4b — compliance `PASS` (initial blocking: transitive vs explicit composite storage → explicit `shared_ptr<StructArray>` members added) · code-review `clean` (1 self-resolved P2).  (full: `plans/reviews/RBA-4a-*.md`, `plans/reviews/RBA-4b-*.md`)
**Verification:** inner-loop `PASS` — Accessor ctest 8/8; integration suite 70/70; RBA-1 no-drift + RBA-2 scalar + RBA-3 metadata all 🟢. Full round-wide suite deferred to a later stage-close (Rust crate not built until RBA-5). (accepted residuals: none)
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor` (RBA-4a `985165e`; RBA-4b this commit)
**Carry-forwards / stop-and-asks:** **[RBA-5/6]** the Rust side must honor the per-language `StructArray` windowing rule (Rust `from_struct` MUST slice children; C++ `field(i)` must not — see RBA-2 note) and the explicit-handle storage model. **[RBA-7]** the oracle spec §7 still shows `field(i)->Slice(...)` for C++ — correct it to "field(i) is already windowed; do not re-Slice" in the RBA-7 docs pass.

## RBA-5 — Rust scalar accessor + Cargo test crate (arrow-rs) (2026-06-24)

**Forcing test:** `scalar_columns_read_and_validate_positionally` (`cargo test`) → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-5-rust-scalar-crate.md`  ·  Step-2: APPROVE. (Process note: the original design doc was accidentally deleted by a Codex-agent tooling bug mid-edit; re-authored complete via a Claude agent and re-APPROVED. Codex design agents repeatedly choked on the large Write — Claude handled it.)
**What landed:** The repo's first Rust crate + the real Rust accessor emitter.
- **Emitter** (`EmitRustAccessor` in `recordbatch_accessor_emitter.cpp`): real `<Class>Accessor` mirroring C++ (D-RBA-8) — `try_new(RecordBatch)` + `from_struct(&StructArray)` sharing `from_columns`; type-only `DataType` gate + `null_count()==0` for proto-non-nullable; offset-preserving `arrow::array::downcast_array::<T>` (D-RBA-3); `Result<_, ArrowError>`, never panics (D-RBA-4); getters utf8→`&str`, binary→`&[u8]`, nullable→`Option`; timestamp/duration unit parsed **generically** from `arrow_type_expr` (getter → i64). `from_struct` **slices each child** to `[offset,len)` (arrow-rs columns NOT pre-windowed — opposite of C++). Composites fail-fast (RBA-6). Generated accessors are **self-contained** (fully-qualified paths + per-`impl` associated `downcast_array`) to avoid name collisions when D-RBA-10's assembler co-mounts same-package files. Reserved package/field segments → `r#<kw>` when raw-able, **generation error** for `crate`/`self`/`Self`/`super` (no silent rename).
- **New crate** `integration-tests/protoc-gen-fletcher-rust/`: `Cargo.toml` (pins `arrow = "=59.0.0"` — verified, no repin), `build.rs` (authoritative `PROTOC`/`FLETCHER_PROTOC_PLUGIN` discovery + loud-only local-dev fallback + clear failure; runs `--fletcher_opt=rust` into `OUT_DIR`; WKT include auto-derived from protoc; **D-RBA-10 module assembler** declaring each package `mod` once), `src/lib.rs`, fixtures (`telemetry`, `sensors_a`/`sensors_b` same-package multi-file), `tests/scalar_columns.rs`, `rust-toolchain.toml`, `.gitignore` (excludes `target/`), `Cargo.lock`.
- **CI**: new `.github/workflows/ci.integration-test.protoc-gen-fletcher-rust.yml` (builds the plugin first, sets the discovery env vars, then `cargo test`), registered in `ci.pr.yml`.
- **Authorized RBA-1 test edit:** removed the bare-`rustc` parse of the now arrow-dependent `.fletcher.rs` from `test_accessor.cpp` (it can't resolve `use arrow::…`; RBA-1 explicitly deferred the real Rust compile to RBA-5). **Byte-identity + "+2 files" assertions unchanged and green** — D-RBA-1 intact. `GeneratedRustFileParsesWithRustc` → documented skip pointing at the cargo crate (now the authoritative Rust compile). PM-authorized; preserves the no-drift guarantee.
**Files touched:** `protoc/src/recordbatch_accessor_emitter.cpp`; `integration-tests/protoc-arrow-bridge/tests/test_accessor.cpp`; `.github/workflows/ci.pr.yml`; new `.github/workflows/ci.integration-test.protoc-gen-fletcher-rust.yml`; new crate `integration-tests/protoc-gen-fletcher-rust/`.
**Reviews:** compliance `PASS` (initial: silent build.rs fallback → made env-authoritative/loud-fallback; silent reserved-ident rename → `r#`/fail-generation) · code-review `blocking 0 (CI-yml-untracked = staging) / major 0 (temporal coverage gap → timestamp+duration added to fixture+test) / minors+nits acceptable`  (full: `plans/reviews/RBA-5-conformance.md`, `plans/reviews/RBA-5-codereview.md`)
**Verification:** inner-loop `PASS` — `cargo test` 4/4 (incl. forcing test + temporal + non-zero-offset `from_struct`); C++ Accessor ctest 7 pass + 1 documented skip + 0 fail (RBA-1 no-drift + RBA-2/3/4 green); plugin unit 3/3. Full round-wide suite + Rust CI run on push (DoD gate at RBA-7). (accepted residuals: none)
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor`
**Carry-forwards / stop-and-asks:** **[RBA-6]** (a) implement the cross-file **package-keyed** two-file/two-package *import* fixture — RBA-5 ships a same-package two-file case (`sensors_a`/`sensors_b`); the two-*package* import needs a nested field, so it was consciously deferred to RBA-6 (reconciles spec §8.1). (b) metadata getters + composite parity in Rust; honor the C++-`nullptr` ≡ Rust-empty-map metadata representation for the eventual parity check.

## RBA-6 — Rust generic metadata + full type parity (2026-06-24)

**Forcing test:** `composite_and_metadata_read` (`cargo test`) → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-6-rust-composite-metadata.md`  ·  Step-2: APPROVE (after 1 rework cycle — R1–R3: fully-qualified `__rba` paths / `pub mod __rba` / plugin-owns-`__rba`-emission; RBA-6a/6b split ENDORSED)
**What landed:** Full Rust composite parity + generic metadata, in two test-first passes (mirrors C++ RBA-3 + RBA-4):
- **RBA-6a** (commit `c9bcaae`): generic metadata getters (`schema_metadata`/`field_metadata` → `&HashMap`, empty when absent, no domain keys, D-RBA-5); STRUCT (incl. ≥2-level cross-file/cross-**package**), REPEATED_SCALAR, REPEATED_STRUCT; `RowAccess` trait + `<Cls>Row`; the plugin-emitted `pub mod __rba` helper (emitted once per protoc run from `GenerateAll`, `include!`d once under `crate::fletcher_gen::__rba`, fully-qualified refs only — no per-file `use`); cross-file **package**-keyed module resolution (`crate::fletcher_gen::<pkg-path>::<Class>Accessor`; no-package → directly under `fletcher_gen`); two-file/two-package + no-package import fixtures. `from_struct` slices children + sliced validity handle; null gate recursed; never panic.
- **RBA-6b** (this commit): MAP scalar-value (`ScalarMapSpan`) + message-value (`StructMapSpan`); NESTED_LIST depth-2 **and** depth-3 (`NestedStructSpan`/`NestedStructSpan3`); struct map-values / list elements → `Option<Row>` (None on null, B2); MapArray bounds via `MapArray::value_offsets()`; the composite support gate made **transitive** (a composite is supported only if its inner message accessor is — prevents a dangling reference to a skipped accessor; the Rust skip-model analogue of C++'s always-emit-class + runtime fail-fast, D-RBA-8).
**Files touched:** `protoc/src/recordbatch_accessor_emitter.cpp` (+`.hpp`), `protoc/src/generator.cpp` (+`generator.hpp`, `__rba` once via `GenerateAll`), `integration-tests/protoc-gen-fletcher-rust/{build.rs, proto/{geo_child,composite_main,composite_aux,nopkg_child,transitive_gate}.proto, tests/{composite_columns,multi_file_invocation,transitive_gate}.rs}`, `integration-tests/protoc-arrow-bridge/tests/test_accessor.cpp` (no-drift +2→+3).
**Reviews:** RBA-6a — compliance `CONFORMANT` (initial: no-package double-colon path → fixed) · code-review `resolved` (P1 multi-file `__rba` dup → `GenerateAll`; P2a/P2b sliced-offset → confirmed false via empirical offset-2 test). RBA-6b — compliance `CONFORMANT` · code-review `resolved` (2×P2 non-transitive support gate → made transitive + depth-4 unsupported-leaf regression test).  (full: `plans/reviews/RBA-6a-*.md`, `plans/reviews/RBA-6b-*.md`)
**Verification:** inner-loop `PASS` — `cargo test` 9/9 (full `composite_and_metadata_read` covering every composite kind + null paths + metadata; cross-package, no-package, multi-file-invocation, non-zero-offset sliced `from_struct`, transitive-gate); C++ Accessor ctest 8/8 (1 documented skip) + full integration suite 70/70; RBA-1 no-drift(+3) + RBA-5 scalar green; protoc unit 3/3. (accepted residuals: none)
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor` (RBA-6a `c9bcaae`; RBA-6b this commit)
**Carry-forwards / stop-and-asks:** **[RBA-7]** docs (README options + accessor section, additively); the cross-language **capstone** (`accessor_cpp_and_rust_agree_on_same_batch`) — honor C++-`nullptr` ≡ Rust-empty-map metadata equivalence; correct the oracle spec §7 `field(i)->Slice` C++ text; re-run RBA-1 no-drift as the round-close regression guard; the round-wide full suite (core unit tests + Rust CI) is the DoD gate.

## RBA-7 — Docs + cross-language capstone parity (2026-06-24)

**Forcing test:** `accessor_cpp_and_rust_agree_on_same_batch` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/RBA-7-docs-capstone-parity.md`  ·  Step-2: APPROVE (after 1 rework cycle — single-source-of-truth expected artifact; explicit scalar-element-null normalization; **1 STOP-AND-ASK → user-authorized**: correcting the `field(i)` windowing guidance in spec §7 AND locked decision D-RBA-7)
**What landed:**
- **Capstone** (`accessor_cpp_and_rust_agree_on_same_batch`): paired C++ gtest + Rust cargo test over ONE shared fixture proto + ONE committed `accessor_capstone_fixture.json` + ONE committed `accessor_capstone_expected.json`; both languages build the batch, read every field kind through their accessor, normalize to a common JSON shape, and assert against the same oracle (each == oracle ⟹ C++ == Rust, D-RBA-8). Every field kind + a representative null in each optional path (null struct row/element, null map message value, null inner list, null scalar element) + schema/field metadata, with C++-`nullptr` ≡ Rust-empty-map normalized to `{}` and asserted **explicitly** in both languages. Proven **non-vacuous**: perturbing the oracle (a data value AND a metadata value) fails BOTH languages.
- **Docs (additive):** `protoc/README.md` (accessor/`rust` options + a "RecordBatch accessors" usage section, with a compile-checked example guarded by `test_accessor_readme_example.cpp`).
- **Doc corrections (user-authorized, factual-mechanism only, no code change):** `docs/recordbatch-accessor-spec.md` §7 and `plans/RBA-locked-decisions.md` **D-RBA-7** — Arrow C++ `StructArray::field(i)` is already windowed to `[offset,len)` (do NOT re-`Slice`; a re-slice reads the wrong row, proven RBA-2/4); arrow-rs `columns()` is NOT pre-windowed (Rust `from_struct` MUST slice). Intent preserved; the wrong wording + its STOP-AND-ASK trigger removed.
**Files touched:** new `integration-tests/accessor-capstone/{proto/accessor_capstone.proto, fixtures/{accessor_capstone_fixture,accessor_capstone_expected}.json, README.md}`, `integration-tests/protoc-arrow-bridge/tests/{test_accessor_capstone.cpp, test_accessor_readme_example.cpp}`, `integration-tests/protoc-gen-fletcher-rust/tests/accessor_capstone.rs`; modified `integration-tests/protoc-arrow-bridge/{CMakeLists.txt, conanfile.py}` (test-only `nlohmann_json`), `integration-tests/protoc-gen-fletcher-rust/{build.rs, Cargo.toml, Cargo.lock}`, `protoc/README.md`, `docs/recordbatch-accessor-spec.md`, `plans/RBA-locked-decisions.md`.
**Reviews:** compliance `PASS` (capstone proves D-RBA-8 transitively + non-vacuously; D-RBA-7 correction user-authorized + intent-preserved; pure add-on; deviations — test-only `nlohmann_json`, flatten-wrapper NESTED_LIST — acceptable) · code-review `blocking 0 (untracked-files = staging) / should-fix 1 RESOLVED (explicit metadata equivalence assertions added) / fragility: none`  (full: `plans/reviews/RBA-7-conformance.md`, `plans/reviews/RBA-7-codereview.md`)
**Verification:** inner-loop `PASS` — C++ Accessor ctest 10/10 (1 documented skip); Rust cargo 10/10. **Round-wide full suite (DoD gate) PASS:** core 25/25 · arrow-bridge 30/30 · pubsub 16/16 · pubsub-arrow 15/15 · protoc 3/3 · full integration ctest 72/72 · Rust crate 10/10; RBA-1 no-drift green; no regressions (`known_accepted_failures: none`).
**Commit / push:** `feature/recordbatchaccessor` → `origin feature/recordbatchaccessor`
**Carry-forwards / stop-and-asks:** none — **round complete.** PR/merge (rebase onto `main`) is the user's step.
