# GIR — Progress Log

One section is appended per item by the runbook after it is green, reviewed,
logged, and pushed. See
[GIR-generator-ir-rewrite.md](GIR-generator-ir-rewrite.md) for the tracker.

<!-- Entries appended below by the round runbook -->

## GIR-1 — Phase 3a: generator compile-and-run harness (2026-07-10)

**Forcing test:** `CoverageHarness.GeneratedCppCompilesEncodesAndReconstructs` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-1-compile-and-run-harness.md`  ·  Step-2: APPROVE (1 rework cycle — Linux preset, graceful-skip, CI wiring)
**What landed:** A greenfield `integration-tests/protoc-coverage/` harness that runs `protoc-gen-fletcher` on a broad `coverage.proto` (one `--fletcher_opt=ipc,accessor,ts,rust` invocation) and actually **compiles + executes** the generated surfaces — C++ edge encode/decode, Arrow view + `ToArrowRow`, IPC-schema readability, RBA C++ accessor (read-only) — building a row and reconstructing it with value-equality. TS/Rust compile-checks report **Skipped** when the toolchain is absent. This is the guard GIR-2's oracles and the GIR-3..7 migrations lean on.
**Files touched:** `integration-tests/protoc-coverage/**` (`coverage.proto`, `coverage_future.proto`, `tests/{test_coverage_harness,test_coverage_accessor}.cpp`, `coverage_fixture.hpp`, `ts/**`, `rust-accessor/**`, `cmake/{run_tsc_check,run_cargo_check,validate_generated_ipc}.cmake`, `CMakeLists.txt`, `conanfile.py`, `README`); `.github/workflows/ci.integration-test.protoc-coverage.yml` (new) + `.github/workflows/ci.pr.yml` (caller: path filter + devcontainer-image + sparse-checkout). No production generator/component source touched.
**Reviews:** compliance `CONFORMS` (0 blocking) · code-review `blocking 4→0 (resolved+re-verified) / non-blocking 3 (1 folded, 2 deferred) / nit 3`  (full: `plans/reviews/GIR-1-conformance.md`, `plans/reviews/GIR-1-codereview.md`, `plans/reviews/GIR-1-codereview-fix-verification.md`)
**Verification:** inner-loop `PASS` (coverage ctest 4/4) · full suite `PASS @ GIR-1 checkpoint` — all 7 components + protoc-arrow-bridge + protoc-coverage harnesses + Rust crate green, zero build errors  (accepted residuals: `AccessorTest.GeneratedRustFileParsesWithRustc` Skipped, `CoverageHarness.GeneratedTypescriptCompiles` Skipped — tsc/rustc absent on box)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **Latent RBA fragility (for RIR):** a proto field literally named `row` collides with the RBA accessor's internal `row` index → generated accessor won't compile. Worked around fixture-only (`ServiceRequest.row`→`payload`; field number/type unchanged, wire-neutral; RBA emitter left read-only per decision #3). Flag for the RBA↔IR reconciliation round.
- **`coverage_future.proto`** parks the scalar-leaf nested-list / flatten family the current generator can't faithfully emit (non-compiling / silently-collapsing / TS-syntax-error cases); GIR-10 enables them.
- **Deferred non-blocking review items:** #5 (function-local `static` in `test_coverage_harness.cpp`), #6 (`cargo --locked` — already honored). One message-clarity nit in `validate_generated_ipc.cmake`.
- **CI:** protoc-coverage is the first integration-test Windows job to use an **absolute** Conan-profile path (more robust); siblings still use relative — consider propagating (out of GIR-1 scope).

## GIR-2 — Phase 3b: encode byte-identity + decode round-trip oracles (2026-07-10)

**Forcing test:** `ParityOracle.EncodeEqualsEncodeRowAndRoundTrips` → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-2-parity-oracles.md`  ·  Step-2: APPROVE (1 rework cycle — golden-*encode* anchoring, non-sorted maps, both-state nullables)
**What landed:** A parity-oracle TU (`test_parity_oracle.cpp`) in the GIR-1 harness that PINS the generated wire contract across 12 fixtures: per fixture it asserts `Encode() == golden` AND `EncodeRow(ToArrowRow()) == golden` AND `Encode()==EncodeRow()` (so a *coordinated* encode drift is caught), plus encode→decode→value-equality and decode-of-known-golden reading back map keys/values + struct inner fields. This is the standing guard every GIR-3..7 emitter migration must keep green. **The oracle immediately caught a real memory-corruption bug** and its fix was folded in (below).
**Wire-corruption fix folded in (locked decision #6):** `protoc/src/generator.cpp:1590` — the nullable-`bytes` `WriteBinary` branch emitted a stray `*` (`reinterpret_cast<const uint8_t*>(*x->data())`), dereferencing the `const char*` to a byte then reinterpreting that value as a pointer → garbage reads / SEH crash for any SET optional/wrapped `bytes` field. `Encode()` was corrupt while runtime `EncodeRow()` was correct — a shipping `Encode()≠EncodeRow()` wire defect. Removed the `*` (matches the non-nullable branch); no sibling antipattern found. Goldens baselined against the CORRECTED `Encode()`. **Surfaced as a stop-and-ask; maintainer authorized the fix.**
**Files touched:** `protoc/src/generator.cpp` (1-site fix); `integration-tests/protoc-coverage/tests/{test_parity_oracle.cpp (new), coverage_test_helpers.hpp (new), coverage_fixture.hpp (extended)}`; `integration-tests/protoc-coverage/CMakeLists.txt` (new `coverage_parity_oracle_tests` target); `integration-tests/protoc-coverage/golden/` (README + 12 tracked `.bin`). RBA emitter untouched.
**Reviews:** compliance `CONFORMS` (0 blocking) · code-review `blocking 0 / non-blocking 0 / nit 0`  (full: `plans/reviews/GIR-2-conformance.md`, `plans/reviews/GIR-2-codereview.md`)
**Verification:** inner-loop `PASS` (protoc unit suite 45/45 post-fix; coverage ctest 6/6) · full suite `PASS @ GIR-2 checkpoint` — all 7 components + protoc-arrow-bridge (72/72) + protoc-coverage (6/6) + Rust crate green against the fixed generator, zero errors  (accepted residuals: `GeneratedRustFileParsesWithRustc` + `GeneratedTypescriptCompiles` Skipped (rustc/tsc absent); `ParityOracle.RegenerateGoldens` gated-Skip)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **Golden re-baseline discipline (for GIR-3..7):** goldens are the wire contract. A migration causing golden churn is a wire change → re-baseline only under review via the gated `FLETCHER_REGEN_PARITY_GOLDENS=1` `RegenerateGoldens` path; a byte change for a previously-supported input is a stop-and-ask (decision #2).
- **`MakeScalarsAllSet()`** leaves `optional_int32`/`wrapped_int32` unset (base sets them) — both presence axes covered per field (re-review-blessed); minor: those two fields' unset path is covered only by-class.
- The nullable-`bytes` fix is a genuine bug in shipping `main`; note for release notes / any downstream that encoded optional `bytes` via the edge path (it would have crashed, so unlikely relied upon).

## GIR-3 — Language-neutral IR + edge encode vertical slice (2026-07-10)

**Forcing test:** `IrTest.BuildsLanguageNeutralIr` → 🟢 (+ GIR-2 encode oracle stays byte-identical)   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-3-language-neutral-ir-edge-encode.md`  ·  Step-2: APPROVE (1 rework cycle — bridge single-source + RBA/TS gate, optional-storage access mode, single-home FieldFacts; + 2 surgical impl notes applied)
**What landed:** The round's centerpiece — a recursive, **language-neutral** type IR (`Scalar(logical-kind, nullable, [enum-identity], [dict-modifier]) | List | FixedSizeList | Struct | Map | Unsupported{reason}`) built in `type_mapper`, carrying only abstract logical identity (decision #1); all C++ type strings moved to a `cpp_backend` lookup keyed on logical id. The edge C++ **encode** emitter is now a recursive visitor over the IR, byte-identical to before. A single-canonical IR→`FieldMapping` bridge (`MapField() = ProjectIrToFieldMapping(BuildFieldIr(...))`) keeps the not-yet-migrated paths (RBA read-only, decode/schema/view/TS) working; `FieldKind` retained.
**Files touched:** new `protoc/include/ir.hpp` + `protoc/src/ir.cpp` (IR), `protoc/include/cpp_backend_type_table.hpp` + `protoc/src/cpp_backend_type_table.cpp` (C++-string backend lookup), `protoc/tests/test_ir.cpp` (forcing test); modified `protoc/src/type_mapper.cpp`, `protoc/include/type_mapper.hpp`, `protoc/src/generator.cpp` (encode visitor + bridge), `protoc/include/generator_internal.hpp`, `protoc/CMakeLists.txt`, `protoc/tests/CMakeLists.txt`. RBA emitter untouched; no `golden/*.bin` changed.
**Reviews:** compliance `CONFORMS` (0 blocking; decision #1 upheld, byte-identity, enum/dict, RBA read-only, single-source bridge, surgical fixes applied) · code-review `2 blocking → both PM-verified FALSE POSITIVES` (P1: files exist, cache-independent rebuild passed 48/48; P2: active nullable flatten-wrapped struct list is set+byte-verified by the oracle and the visitor derefs the optional correctly — the scalar-leaf variant is the pre-existing parked GIR-10 limitation)  (full: `plans/reviews/GIR-3-conformance.md`, `plans/reviews/GIR-3-codereview.md` incl. PM verification note)
**Verification:** three-gate cutover, all green — Gate A protoc unit suite **48/48** (forced rebuild `--build=fletcher-protoc/*` to defeat the run_tests cache-mask; incl. `IrTest.BuildsLanguageNeutralIr`, `IrTest.CppBackendLookupOwnsTypeStrings`, `IrTest.MapFieldEqualsProjectionOfBuildFieldIr`) · Gate B coverage harness **6/6** (encode parity oracle byte-identical) · Gate C Rust no-drift green. NOT a full-suite item (GIR-4 is the interim checkpoint).  (accepted residuals: TS + rustc-parse Skipped where absent; `RegenerateGoldens` gated-Skip)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **Recovery note:** the implementer agent was killed by the stream watchdog after the **machine slept** mid-run (not a code/build fault). The full implementation had landed; I reconciled the working tree and ran the gates myself. **Red-first could not be independently re-confirmed** (agent died before reporting) — mitigated by both reviews judging `test_ir.cpp` SUBSTANTIVE (constructs real IR + asserts concrete facts incl. the decision-#1 no-C++-strings check).
- **Conan cache-mask gotcha (reusable):** `protoc/conanfile.py` `package_id()` ignores `run_tests`, so a `run_tests=False` cached package satisfies a `run_tests=True` request as "Already installed" and the unit suite silently does NOT run. To actually run it, force `conan create . --build=fletcher-protoc/* -o "&:run_tests=True"`.
- **Bridge is temporary (for GIR-4..7 + RIR):** `ProjectIrToFieldMapping`/`FieldKind` retire as decode/schema/view/TS migrate onto the IR (GIR-4..7) and RBA reconciles in RIR. Do not let RBA/IPC take a permanent dependence on the adapter (decision #4).

## GIR-4 — Edge decode emitter on the IR (2026-07-10)

**Forcing test:** `CoverageHarness.GeneratedCppCompilesEncodesAndReconstructs` (broadened to all active fixtures + committed goldens, via the edge decode ctor) → 🟢   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-4-edge-decode-on-ir.md`  ·  Step-2: APPROVE (2 rework cycles — Codex first hallucinated the decode API + named the wrong forcing test; reworked against the real `EmitFieldDecode`, then transcribed exact emitted shapes from generator.cpp line-by-line)
**What landed:** The edge C++ **decode** emitter (`EmitFieldDecode`, the sequential `PositionalReader`) is now a recursive IR visitor (`cpp_backend::EmitFieldDecodeFromIr`), byte/value-identical to the old `FieldMapping`-switch decoder. Read-method names come from the C++-backend table (`CppScalarInfo::positional_read`) — no read-method/type string on any IR node (decision #1). After GIR-4, **encode + decode are both direct IR emitters**; schema/IPC, view, TS, RBA still ride the IR→FieldMapping bridge (GIR-5/6/7 + RIR).
**Forcing-test correction (plan refinement):** the tracker originally named "GIR-2 decode round-trip oracle" as GIR-4's guard, but that oracle decodes via the runtime `Codec::DecodeRow` + Arrow `View` (a GIR-6 surface) and stays green regardless of the edge decoder — it does NOT guard `EmitFieldDecode`. The real guard is the compile-and-reconstruct harness through the edge ctor; broadened to all fixtures + goldens this item. Tracker cell updated.
**Files touched:** new `protoc/include/cpp_backend_decode_visitor.hpp` + `protoc/src/cpp_backend_decode_visitor.cpp` (`EmitFieldDecodeFromIr`); `protoc/src/generator.cpp` (both decode ctors routed to the visitor; dead `EmitFieldDecode`/`PositionalReadCall` removed, ~177 lines); `protoc/CMakeLists.txt`; `protoc/tests/test_ir.cpp` (new `IrTest.EdgeDecodeVisitorEmitsPositionalReads`); `integration-tests/protoc-coverage/tests/test_coverage_harness.cpp` (+ CMakeLists — broadened reconstruct over all fixtures + `PARITY_GOLDEN_DIR_PATH`). RBA untouched; no `golden/*.bin` changed.
**Reviews:** compliance `PASS` (0 blocking; decision #1, byte/value identity, scope, clean dead-code removal, substantive tests) · code-review `1 "blocking" → FALSE POSITIVE` (untracked new files absent from `git diff`; files exist, build passed 49/49, reviewer read them and confirmed all logic PASS — map `ReadMapValueBitfield` cursor-sync present, nested-list fresh vars, owned copies, dead-code clean). `git add`ed at commit.  (full: `plans/reviews/GIR-4-conformance.md`, `plans/reviews/GIR-4-codereview.md`)
**Verification:** three-gate cutover green — protoc unit suite **49/49** (forced rebuild; incl. all 4 IR tests + the new decode assertions) · coverage harness reconstruct + encode parity oracle byte-identical · RBA no-drift green · **interim full-suite checkpoint PASS** (all 7 components + protoc-arrow-bridge 72/72 + protoc-coverage 6/6 + Rust crate; gap since GIR-2 full suite = GIR-3+GIR-4 = 2 items, bounded & clean).  (accepted residuals: rustc-parse + TS Skipped where absent; `RegenerateGoldens` gated-Skip)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **Codex-vs-real-API friction (process note for GIR-5..7):** Codex twice approximated `EmitFieldDecode`'s exact emitted shapes; converged only when told to OPEN generator.cpp at cited lines and transcribe. For the remaining emitter migrations, brief the architect to read the real emitter verbatim, and lean on the compile+reconstruct gate as ground truth.
- **`positional_read` is now a stored C++-backend-table field** (`CppScalarInfo::positional_read`), replacing the old call-time `PositionalReadCall` derivation — a backend concern, not on the IR (decision #1 intact).
- The recurring 4b "untracked files missing" false positive: reviewers run `git diff <base>` which omits `??` files — always cross-check `git status` + read new files; `git add` them at commit.

## GIR-5 — Unified schema + IPC visitor (2026-07-10)

**Forcing test:** `SchemaVisitor.CppAndIpcByteIdentical` → 🟢 (+ `test_schema_builder` green + per-node-kind `.ipc` goldens byte-identical)   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-5-unified-schema-ipc-visitor.md`  ·  Step-2: APPROVE (2 rework cycles — flatten/field_id not in the IR → shared FieldMapping-free flatten walk; SchemaSink/child-nav abstraction; per-node `.ipc` goldens as the real anti-drift guard (CppAndIpcByteIdentical alone is tautological once unified); then filter-preservation of UNSUPPORTED/FIXED_SIZE_LIST + `"item"`-restore-after-deep-copy)
**What landed (locked decision #5):** BOTH schema paths now run through ONE IR `SchemaVisitor` with two sinks — `GenerateSchemaFunction`→`GenerateSchemaFunctionFromIr` (`CppSchemaSink` renders C++ source) and `BuildMessageSchemaInto`→`BuildMessageSchemaIntoFromIr` (`NanoarrowSchemaSink` executes in-process/`.ipc`). Kills the hand-maintained nanoarrow/IPC lockstep. A shared, language-neutral `BuildFlattenedFieldList` mirrors `GatherFieldsImpl` (dotted `field_id`, leaf `field_number`) and reproduces the exact UNSUPPORTED/FIXED_SIZE_LIST/non-representable filter. Timestamp NANO, enum→INT32, map child-name rules, metadata content+order, nullability, `"item"`-restore all preserved.
**Files touched:** new `protoc/include/cpp_backend_schema_visitor.hpp` + `protoc/src/cpp_backend_schema_visitor.cpp` (sinks + visitor + flatten walk), `protoc/tests/test_schema_visitor.cpp` (forcing test + env-guarded golden capture), `protoc/tests/golden/*.ipc` (10 committed per-node-kind goldens from today's emitter); modified `protoc/src/generator.cpp` (both emitters → thin wrappers over the visitor), `protoc/include/schema_builder.hpp`, `protoc/include/generator_internal.hpp` (doc), `protoc/CMakeLists.txt`, `protoc/tests/CMakeLists.txt` (+ `SCHEMA_GOLDEN_DIR`). RBA/FieldKind untouched.
**Reviews:** compliance `CONFORMS` (0 blocking; both paths unified, goldens byte-identical, filter preserved, IR language-neutral, cosmetic churn semantically equivalent, RBA read-only) · code-review `1 "blocking" → FALSE POSITIVE` (untracked new files absent from `git diff`; all 7 detailed checks PASS incl. sinks-emit-identical via a recording-decorator test, nanoarrow RAII lifetime, dead-code cleanup). `git add`ed at commit.  (full: `plans/reviews/GIR-5-conformance.md`, `plans/reviews/GIR-5-codereview.md`)
**Verification:** three-gate cutover green — protoc unit **51/51** (forced rebuild; incl. `SchemaVisitor.CppAndIpcByteIdentical`, `test_schema_builder`, `test_ir`, `test_type_mapper`) · coverage harness (encode/decode + parity oracles + accessor) green · RBA no-drift green. **Strong no-drift proof:** implementer built the base-commit plugin in a worktree and byte-diffed all 9 coverage `.ipc` — byte-identical. NOT a full-suite item (GIR-7 is the cluster checkpoint).  (accepted residuals: TS/rustc-parse Skipped where absent)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **Generated schema C++ SOURCE churns cosmetically** (`schema->`→`schema.get()->`, dropped `// Warning:` comments, list `SetName("item")` reordered) — all formatting-equivalent, PROVEN by byte-identical `.ipc` (no committed source golden exists to update). If a generated-source golden is added later, expect this one-time churn.
- After GIR-5, **encode + decode + schema/IPC are direct IR emitters**; view (GIR-6), TS (GIR-7), RBA (RIR) still ride the IR→FieldMapping bridge.
- `GatherFieldsImpl`/`EmitNanoarrowTypeSetup`/`SetScalarSchemaType` retained (still used by other not-yet-migrated emitters); they retire as GIR-6/7 land.

## GIR-6 — Arrow view getters + ToArrowRow on the IR (2026-07-10)

**Forcing test:** `ViewVisitor.RoundTripsViaCodec` → 🟢 (message → `ToArrowRow` → Arrow → view getters → reconstruct → `Equals`, over the real `CompositeCoverage` + active fixtures, reading back map keys/values + struct inner fields)   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-6-arrow-view-toarrowrow-on-ir.md`  ·  Step-2: APPROVE (1 rework cycle — scope correction: `SetFrom*`/`ToScalars`/`Make*Scalar_` were DEAD code (0 callers/0 generated hits), so migrate only the live getters + `ToArrowRow` and DELETE the dead helpers; nested-list whole-shape depth-2/3; no double-default; forcing test homed in the integration harness)
**What landed:** The live Arrow **view getters** + **`ToArrowRow`** migrated off the IR→FieldMapping bridge onto IR visitors (`EmitViewGetterFromIr` + `EmitToArrowRowFieldFromIr` in new `cpp_backend_view_visitor.{hpp,cpp}`), value/byte-identical. Nested-list reproduces the real whole-shape depth-2 (`ArrowNestedList`) / depth-3 (`ArrowNestedList2`) emission via structural depth counting to leaf-struct identity (no `list_depth` in IR). `array_type`+`getter_type` added to `CppScalarInfo` (backend table only — no Arrow/C++ string on IR nodes, #1). **Dead-code deleted** (0-caller-verified): `EmitScalarHelper`(`Make*Scalar_`), `ScalarEntry`(`ToScalars`), `EmitFieldExtraction`(`SetFromScalars_`), + orphaned `ReplaceAll`/`ArrayTypeFromScalar`/`GetterType`.
**Files touched:** new `protoc/include/cpp_backend_view_visitor.hpp` + `protoc/src/cpp_backend_view_visitor.cpp`; new `integration-tests/protoc-coverage/tests/test_view_visitor.cpp` (forcing test); modified `protoc/include/cpp_backend_type_table.hpp`, `protoc/src/cpp_backend_type_table.cpp`, `protoc/src/generator.cpp` (view/ToArrowRow → visitors; dead helpers removed), `protoc/CMakeLists.txt`, `integration-tests/protoc-coverage/CMakeLists.txt`. RBA/FieldKind untouched; schema visitor untouched (`.ipc` byte-identical).
**Reviews:** compliance `PASS` (7/7 pressure tests; value/byte identity, no IR pollution, dead-code 0-caller, nested-list whole-shape, no double-default, scope/bridge, unit-count decrease legitimate) · code-review `PASS` (0 blocking / 0 non-blocking / 0 nit — byte-identical across all node kinds, `ClassifyNestedList` depth correct, all 13 scalar kinds mapped, memory-safe zero-copy getters vs owned ToArrowRow, substantive round-trip test).  (full: `plans/reviews/GIR-6-conformance.md`, `plans/reviews/GIR-6-codereview.md`)
  - Process note: the recurring 4b/4a "untracked files missing" false positive (a compliance pass even bailed on it) is fixed by **`git add`-staging new files BEFORE dispatching reviews** — staged files appear in `git diff <base>`, unlike untracked ones. Adopt for GIR-7+.
**Verification:** three-gate cutover green — `ViewVisitor.RoundTripsViaCodec` 🟢 · coverage integration 7/7 (parity oracle encode/decode/golden byte-identity, accessor, harness incl. schema `.ipc` oracles, Rust accessor) · protoc unit suite green · RBA no-drift green. Parity oracle proves `Codec.EncodeRow(ToArrowRow())==golden` for every fixture → no view-value/golden byte change. NOT a full-suite item (GIR-7 is the cluster checkpoint).  (accepted residuals: TS/rustc-parse Skipped where absent)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- After GIR-6, **encode + decode + schema/IPC + view are all direct IR emitters.** Only **TS (GIR-7)** and **RBA (RIR)** remain on the IR→FieldMapping bridge. GIR-7 is the last emitter migration before the bridge can largely retire (RBA reconciles in RIR).
- The dead-helper deletion (`SetFrom*`/`ToScalars`/`Make*Scalar_`) removed a generated API surface that was never wired — safe (0 callers, 0 generated hits), but note it in case any external consumer referenced the never-emitted symbols.

## GIR-7 — TS interface + SchemaDescriptor on the IR (2026-07-10)

**Forcing test:** `TsVisitor.DescriptorByteIdentical` → 🟢 (IR-driven TS interface + descriptor byte-identical to a committed frozen golden `coverage.fletcher.ts` for every active fixture) + `tsc --noEmit` (Skips when tsc absent — accepted)   (⚪ → 🔴 → 🟢)
**Design:** `plans/GIR-7-ts-interface-descriptor-on-ir.md`  ·  Step-2: APPROVE (1 rework cycle — use `BuildFlattenedFieldList` not raw IR fields; recover the declared wrapper name for #12/#18 to preserve byte-identity; committed frozen golden not a build artifact; `GenerateFile` details; corrected bridge claim)
**What landed (the last emitter migration):** The TypeScript interface + runtime `SchemaDescriptor`/`TypedSchema` emitter migrated to a direct IR visitor (`ts_backend::TsVisitor`) with a NEW `ts_backend` table (logical id → TS type text + `WireTypeId`). **This is the round's language-neutrality proof-point**: a second, non-C++ backend, with ALL TS strings living only in `ts_backend` (keyed on `ir::LogicalType`+`EnumIdentity`) — none on any IR node. Wrapper-name recovery keeps #12/#18 (singular flatten wrappers) as `IStructListWrapper[]` while #14/#16 (repeated) stay `ILeaf[][]`, via `Descriptor::name()` (a language-neutral proto fact). Import discovery is IR-native (`CollectStructImportFiles` over `BuildFlattenedFieldList` + recursive `IrNode`) — no `MapField`/`FieldKind` in the TS path.
**Files touched:** new `protoc/include/ts_backend_type_table.hpp` + `protoc/src/ts_backend_type_table.cpp`, `protoc/include/ts_backend_visitor.hpp` + `protoc/src/ts_backend_visitor.cpp`; new tests `integration-tests/protoc-coverage/tests/test_ts_visitor.cpp` (byte-identity vs golden) + `protoc/tests/test_ts_visitor.cpp` (unit guard `SingularNestedFlattenWrapperKeepsLeafName`); new committed golden `integration-tests/protoc-coverage/golden/coverage.fletcher.ts`; modified `protoc/src/generator.cpp` (TS → `TsVisitor`; dead TS helpers removed; `ValidateServiceMethod` relocated to extern `fletcher` linkage), `protoc/include/generator_internal.hpp`, `protoc/{include,src}/cpp_backend_schema_visitor.*`, `protoc/CMakeLists.txt`, `protoc/tests/CMakeLists.txt`, `integration-tests/protoc-coverage/CMakeLists.txt`. RBA/FieldKind untouched.
**Reviews:** compliance initially `DEVIATES` (TS import scan still used `MapField`/`FieldKind`) → fixed (IR-native import walk) → re-review `CONFORMS`. code-review `0 blocking / 1 non-blocking (P2) / 0 nit`; P2 (latent wrapper-name propagation through nested lists) FIXED + guarded (unit test, no cross-emitter golden churn). Staged new files before reviews to kill the recurring untracked-files false positive.  (full: `plans/reviews/GIR-7-conformance.md`, `plans/reviews/GIR-7-codereview.md`)
**Verification:** three-gate cutover + **cluster full-suite checkpoint PASS** — protoc unit **52/52** (forced rebuild; all 4 IR tests + `SchemaVisitor.CppAndIpcByteIdentical` + `TsVisitor.SingularNestedFlattenWrapperKeepsLeafName`) · coverage harness **8/8** (`TsVisitor.DescriptorByteIdentical` + view + accessor + parity encode/decode + schema `.ipc` + Rust accessor) · protoc-arrow-bridge 72/72 · all 6 other components + Rust crate green. Golden `.ts` byte-unchanged after the import-scan de-bridge. Gap since the GIR-4 full suite (GIR-5/6/7 = 3 items) bounded & clean.  (accepted residuals: TS + rustc-parse Skipped where absent; `RegenerateGoldens` gated-Skip)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **The emitter-migration spine is complete.** After GIR-7, **encode + decode + schema/IPC + view + TS are ALL direct IR emitters.** `FieldMapping`/`FieldKind` now serve ONLY the RBA accessor emitter + the edge row-class setters/getters — the bridge fully retires in **RIR** (RBA↔IR reconciliation). Remaining round items are features/coverage (GIR-8/9/10/11), not migrations.
- **Review-hygiene fix adopted:** `git add`-stage new files BEFORE dispatching reviews — staged files appear in `git diff <base>`, eliminating the recurring "untracked files missing / build fails" false positive that tripped multiple 4a/4b passes.
- `ValidateServiceMethod` relocated to extern `fletcher` linkage (to be callable from the TS visitor TU) — reviewed, no ODR/behavioural issue.

## GIR-8 — #55 unsupported→build error + #53-generated no-ValueOrDie (2026-07-11)

**Forcing test:** `GenErrors.UnsupportedTypeFailsBuild` (+ `RepeatedUnsupportedTypeFailsBuild`, `MapUnsupportedTypeFailsBuild`) + `GenErrors.NoValueOrDieInEmitterSources` (source-side, all IR emitter TUs) + `GenErrors.NoValueOrDieInIrGeneratedCode` (generated-output spot-check) → all 🟢   (⚪ → 🔴 → 🟢) — RED-FIRST feature/fix (decision #9).
**Design:** `plans/GIR-8-unsupported-error-no-valueordie.md`  ·  Step-2: APPROVE (2 rework cycles — mirror the generator's `IsRecursive||IsFlattenedWrapper` skip so recursion stays non-fatal; exhaustive `FindUnsupportedIr` incl. FIXED_SIZE_LIST; then a prose-consistency fix committing to recursion-unchanged)
**What landed:** (#55) A front-end `ValidateNoUnsupportedIr()` pass (exhaustive `FindUnsupportedIr` over struct/list/fixed-size-list/map) sets protoc `*error` on genuinely-unsupported types (`google.protobuf.Any`/`Struct`, real `oneof`, unsupported map key/value, unsupported flatten-leaf) — a clean build error instead of a silent skip. Applies the generator's skip predicate so **recursion stays skipped/non-fatal** (no regression) and proto2 groups (→INT32) are not flagged. (#53) A checked-result helper `detail::FletcherValueOrThrow<T>()` (throws `std::runtime_error` on `!ok()`, `ValueUnsafe()` on `ok()` → value-identical) replaces all 9 emitter `.ValueOrDie()` sites (7 view + 2 generator).
**Files touched:** `protoc/src/generator.cpp` (Validate/FindUnsupportedIr + 2 sites), `protoc/src/cpp_backend_view_visitor.cpp` (helper + 7 sites), `protoc/src/ir.cpp` (`DynamicWktUnsupportedReason` applied in singular/repeated/map builders so repeated/map Any/Struct are consistently UNSUPPORTED); new `protoc/tests/check_no_value_or_die_in_emitters.cmake` + `protoc/tests/CMakeLists.txt`; new `integration-tests/protoc-coverage/proto/coverage_unsupported{,_repeated,_map}.proto` + `cmake/{run_unsupported_generation_check,check_no_value_or_die}.cmake` + `CMakeLists.txt`. RBA untouched.
**Reviews:** compliance initially blocking (zero-ValueOrDie check under-scoped to protoc-coverage) → fixed with a harness-independent emitter-SOURCE guard (all 13 emitter TUs, RBA excluded) → re-review CONFORMS. code-review `0 blocking / 1 non-blocking P2 (repeated/map Any bypass)` → FIXED via root cause in `ir.cpp` (+ red-first fixtures) → re-review CONFORMS (regression-free; scoped to Any/Struct only).  (full: `plans/reviews/GIR-8-conformance.md`, `plans/reviews/GIR-8-codereview.md`)
**Verification:** three-gate cutover green — protoc unit **53/53** (+ 3 pkg; incl. all `GenErrors.*`) · coverage **12/12** (3 unsupported-fail tests + both no-ValueOrDie checks + all oracles byte-identical: parity encode/decode, view round-trip, TS descriptor, edge/accessor/Rust) · RBA no-drift. NOT a full-suite item; the FletcherValueOrThrow generated-code change across all harnesses is validated at the GIR-9 full-suite checkpoint (2-item window).  (accepted residuals: tsc/rustc-parse Skipped; RegenerateGoldens gated-Skip)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **Env gotcha (for full-suite runs):** the coverage harness's `find_program` can cache a stale plugin path from a prior `build/` dir → picks yesterday's plugin (tests fail with the exact red symptoms). Fix: delete `integration-tests/protoc-coverage/build` and reconfigure. Relevant to the GIR-9 full suite.
- #55 now closes; #53's generated half closes. The generated C++ view source churned (helper substitution) — no committed generated-source golden exists to update; wire/schema/TS/`.ipc` all byte-identical.

## GIR-9 — #75 emit C++ enum symbols (typed accessors) (2026-07-11)

**Forcing test:** `EnumEmit.GeneratedEnumSymbolsRoundTrip` → 🟢 (typed accessor returns the matching enumerator on edge AND view, package-level + nested + standalone enums; wire byte-identical / int32 storage)   (⚪ → 🔴 → 🟢) — RED-FIRST feature (decision #9).
**Design:** `plans/GIR-9-enum-symbols.md`  ·  Step-2: APPROVE (3 rework cycles — enum-owner ordering edge; source enum identity from IR/descriptor not FieldMapping; fluent typed setter; view red-first; class coverage incl. nullable/repeated/map; then separate `enum_coverage.proto` so existing goldens stay byte-identical; emit-ALL-enums per spec)
**What landed (#75):** Every proto enum now emits `enum class <Name> : int32_t { … }` at the correct C++ scope (top-level in the package namespace; nested inside its owning generated class) via a shared `cpp_backend::CppEnumName`. TYPED accessors are ADDED alongside the retained raw int32 ones (typed getters on singular/nullable/repeated/map; fluent `cls&` typed setters on singular/nullable) — value-preserving casts over int32 storage. `TopologicalVisit` gained an enum-owner ordering edge (guarded by a shared in-progress `visiting` set against cycles), and typed-accessor/edge emission is gated by `CppEnumTypeEmittable` (`!IsRecursive(owner) && !IsFlattenedWrapper(owner)`) so a field whose enum-owner is skipped falls back to raw int32 (never references an undeclared class). Storage stays int32 — no wire change.
**Files touched:** `protoc/src/generator.cpp` (enum-class emission, ordering edge + cycle guard, gate, cross-file enum-include collector), `protoc/src/cpp_backend_view_visitor.cpp` (view typed getters + gate), `protoc/src/cpp_backend_type_table.{cpp,hpp}` (`CppEnumName` + `CppEnumTypeEmittable`); new fixture `integration-tests/protoc-coverage/proto/enum_coverage.proto` (EnumCoverage singular/nullable/repeated/map + standalone + skipped/flattened/recursive-owner red-first cases) + `tests/test_enum_emit.cpp` + golden `golden/enum_coverage.fletcher.ts`; harness wiring `CMakeLists.txt` + `cmake/{validate_generated_ipc (stem-aware),run_tsc_check}.cmake`. Existing `coverage.*` goldens byte/set-identical; RBA untouched (enum unit omits `rust` to avoid duplicate `__rba.fletcher.rs`).
**Reviews:** compliance initially blocking twice (enum-field golden tension → separate proto; then owner-emittability gate) → fixed → CONFORMS. code-review `0 blocking / 2 P2` (flattened-imported-enum header; enum-owner cycle infinite-recursion) → both FIXED (field-flatten include descent; shared `visiting` cycle guard) with red-first fixtures → CONFORMS.  (full: `plans/reviews/GIR-9-conformance.md`, `plans/reviews/GIR-9-codereview.md`)
**Verification:** three-gate cutover + **interim full-suite checkpoint PASS** — protoc unit **53/53** · protoc-coverage **14/14** (EnumEmit + enum.TsVisitor + all coverage oracles byte/set-identical) · protoc-arrow-bridge **72/72** · all 6 other components + Rust crate green. Both integration harnesses recompiled generated code carrying GIR-8's `FletcherValueOrThrow` AND GIR-9's typed enum accessors → all green (validates the GIR-8+GIR-9 generated-code window). Ran with the coverage `build/` cleared to defeat the stale-plugin gotcha.  (accepted residuals: tsc/rustc-parse Skipped; RegenerateGoldens gated-Skip)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **#75 closes.** All generator-behaviour fixes (#55, #53-generated, #75) are now landed on the IR emitters (decision #6). Only coverage items (GIR-10/11) remain.
- A genuine enum-owner CYCLE (mutual nested-enum references) is valid proto but fundamentally unorderable in C++ (nested enums can't be forward-declared) — the cycle guard makes protoc terminate deterministically rather than crash; such a proto still can't compile (inherent), which is acceptable (no in-repo proto does this).
- `enum_coverage` has no committed `.v1.bin`/`.ipc` golden — its wire is guarded by the in-memory round-trip in `EnumEmit` + the `.ipc` filename-set guard; sufficient for an additive int32-storage fixture.

## GIR-10 — Codec edge/boundary + flatten/arbitrary-nesting coverage (3c/3d) (2026-07-11)

**Forcing test:** `CodecEdge.*` (23 tests) + `Nesting.ListOfListOfScalarRoundTrips` (+ `SchemaEvolutionNegative{V1AsV2,V2AsV1}`) → all 🟢   (⚪ → 🔴 → 🟢) — COVERAGE item (may-find-bugs / red-first).
**Design:** `plans/GIR-10-codec-edge-nesting-coverage.md`  ·  Step-2: APPROVE (1 rework cycle — new message not widening CompositeCoverage; RBA-safe no-accessor/rust unit; corrected §3d storage attribution; schema-evolution negatives; + a doc-wording fix)
**What landed:** **§3c** — new direct codec-unit TU `arrow-bridge/tests/test_codec_edge.cpp` (23 `CodecEdge.*`): unions (sparse/dense, varlen children), intervals, time32/64, duration, decimal128/256 incl. negative+max, large/view string+binary, fixed-size-binary; boundaries INT*_MIN/UINT*_MAX, NaN/±Inf/-0.0 **bit-exact** (memcpy), embedded-NUL/UTF-8, wide null-bitfields; decode-error paths. **All 23 are regression guards** — the Phase-1-hardened (#98) runtime codec had NO bug, so NO decision-#6 fix and NO wire change. **§3d** — scalar-leaf `List<List<scalar>>` enabled: new `ScalarNestedCoverage` message in new `coverage_scalar_nested.proto`, generated in an `--fletcher_opt=ipc,ts`-only unit (OMITS accessor+rust). The real bug was a *collapse* in `type_mapper.cpp` `BuildFlattenedRepeated` (caller's repeated level dropped) — fixed by a scoped loop-bound `depth→depth+1` (NOT a grammar change); enabled via reusing `FieldKind::NESTED_LIST` + an additive language-neutral `nested_leaf_is_scalar` discriminator (NO new enum value → RBA switch untouched). Emitter fixes: storage (generator.cpp), decode (`cpp_backend_decode_visitor`), view (`cpp_backend_view_visitor` + new `ArrowNestedScalarList`/`2` in `arrow_row_view.hpp`), schema (`cpp_backend_schema_visitor`); TS auto-faithful (`number[][]`/`[][][]`).
**RBA-safety guard (review-driven, locked #3):** both reviews caught that scalar-leaf nested lists could still reach the read-only RBA accessor/rust emitters (struct-leaf assumption → invalid code) if a user ran `--fletcher_opt=accessor`/`rust`. Fixed with a FRONT-END guard `ValidateBackendsSupportFields`/`FindScalarLeafNestedList` (generator.cpp) that fails the plugin with a clear "not supported by the RecordBatch accessor / Rust backend (tracked for RIR)" error BEFORE any emission — RBA emitter untouched. Red-first negative tests `GenErrors.ScalarLeafNestedListRejectedBy_{accessor,rust}`.
**Files touched:** new `arrow-bridge/tests/test_codec_edge.cpp`; new `integration-tests/protoc-coverage/{proto/coverage_scalar_nested.proto, tests/test_nesting.cpp, golden/coverage_scalar_nested.fletcher.ts, cmake/run_backend_guard_check.cmake}`; modified `arrow-bridge/.../arrow_row_view.hpp` (+ArrowNestedScalarList/2), `protoc/src/{ir,type_mapper,generator,cpp_backend_decode_visitor,cpp_backend_view_visitor,cpp_backend_schema_visitor}.cpp`, `type_mapper.hpp`, harness CMakeLists. RBA emitter + existing goldens byte-identical.
**Reviews:** compliance + code-review BOTH flagged one blocking (scalar-leaf nested list reaching read-only RBA) → fixed with the front-end guard → re-review CONFORMS. §3c confirmed all-guards (no wire change); depth+1 fix confirmed scoped (struct-leaf nested lists byte-identical); no false positive (coverage.proto full-opts still exit 0).  (full: `plans/reviews/GIR-10-conformance.md`, `plans/reviews/GIR-10-codereview.md`)
**Verification:** three-gate + arrow-bridge codec gate — arrow-bridge **53/53** (CodecEdge), protoc unit **53/53**, protoc-coverage **20/20** (Nesting + schema-evolution negatives + the 2 backend-guard negatives + all existing oracles byte-identical), RBA `cargo test` no-drift. NOT a mandated full-suite item; round close is the final full suite.  (accepted residuals: tsc/rustc-parse Skipped; gated regen skips)
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **§3c found NO codec bug** — the Phase-1 hardening (#98) holds across the edge/boundary matrix. All 23 are standing regression guards.
- **RIR must extend the RBA accessor/Rust emitters to scalar-leaf nested lists** (currently guarded off with a clean error). Until then, `List<List<scalar>>` protos must omit `--fletcher_opt=accessor,rust`.
- **clang-format 18.1.3 env gotcha** (per memory): local clang-format disagrees with the committed baseline; kept committed style, surgical diff — do not machine-reformat.

## GIR-11 — Property + fuzz (3e) (2026-07-11)

**Forcing test:** `Fuzz.DecodeRowSurvivesRandomTruncatedBuffers` + `Property.EncodeDecodeRoundTrip` → both 🟢   (⚪ → 🔴 → 🟢) — final round item; COVERAGE (may-find-bugs).
**Design:** `plans/GIR-11-property-fuzz.md`  ·  Step-2: APPROVE (1 rework cycle — omit unions from the property corpus to avoid a false Equals-failure; terminal `catch(...)`; portability/hex-repro note; honest type-coverage scope)
**What landed:** A new deterministic, bounded codec-robustness TU `arrow-bridge/tests/test_codec_property_fuzz.cpp` — two standing guards over the Phase-1-hardened (#98) `EncodeRow`/`DecodeRow`. **Property** (`EncodeDecodeRoundTrip`, seed `GIR11_PR`, 128 iters × 2 schemas, recursive ~20% nullability, finite floats, decimal precision ≤18, NO unions): random valid rows satisfy `encode→decode→Equals` field-by-field + encode/decode/encode byte-determinism. **Fuzz** (seed `GIR11_FZ`, 32 seed rows/schema × {256 garbage, every truncated prefix, 256 corruptions}): `DecodeRow` over malformed buffers accepts ONLY success or `std::invalid_argument`; a dedicated `attempt_valid()` path strictly REQUIRES success on the exact-valid seed (any throw = FAIL — catches a decode regression); shared `ctx()`+`SCOPED_TRACE` (seed/schema/variant/case/hex) on every branch; terminal `catch(...)` fails on any other outcome.
**Result:** the fuzz found **NO crash/UB/unexpected-outcome** — Phase-1 decode hardening HOLDS across all strategies. **No production-code change** (codec.cpp/row_reader.hpp untouched); both tests stand as regression guards.
**Files touched:** `arrow-bridge/tests/test_codec_property_fuzz.cpp` (new) + `arrow-bridge/tests/CMakeLists.txt`. RBA untouched.
**Reviews:** compliance initially DEVIATES (valid-variant permissively accepted a rejection → masks a codec regression; size-check lacked repro context) → fixed (`attempt_valid()` strict-success + shared `SCOPED_TRACE`) → re-review CONFORMS. code-review `0/0/0` (confirmed the fuzz is TOOTHED — corruption strategies reach the guarded decode paths; property generator genuine; harness memory-safe).  (full: `plans/reviews/GIR-11-conformance.md`, `plans/reviews/GIR-11-codereview.md`)
**Verification:** arrow-bridge **55/55** (incl. #54 Property + #55 Fuzz); protoc 53/53 sanity (generator unaffected); format-clean. §3e met. NOT a mandated full-suite item; the round-close full suite is the final gate.
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **The GIR round is functionally complete (GIR-1..GIR-11 all 🟢).** The Phase-1 hardening (#98) survived the fuzz — no robustness gap.
- Property corpus deliberately excludes unions + several exotic types (decimal256/FSB/intervals/time/large/view) — covered by GIR-10 `CodecEdge.*`; the property test focuses on the common round-trippable type space.

## GIR-13 — #121 option metadata on the IR schema visitor (2026-08-26)

**Forcing test:** `OptionMetadataTest.FlattenFieldWrapperContextReachesEachInlinedLeaf` → 🟢 — but **the nominal forcing test proved weak**: it goes green on the `protoc/tests/CMakeLists.txt` wiring line ALONE (pure resolver semantics; it never exercises the flatten-chain accumulation). The design predicted this and the implementer confirmed it empirically. The real gates are the design's §6 **T1–T3** guards + the `MetadataOptions`/`MetadataNoDrift`/`IpcParity` integration set. **Lesson for future rounds: an item whose tests already exist needs its forcing test chosen by what the test EXERCISES, not by what it is named after.**
**Design:** `plans/GIR-13-option-metadata-on-ir.md` · Step-2: APPROVE at revision 3 (2 rework cycles; items 10 → 1 → 0 — clean convergence, no root-cause escalation). Cycle-2's blocking find was that T2 was **unsatisfiable as written** — it asserted a `field_type:`-scope key on a scalar leaf, which `option_metadata.cpp:501` skips (`cpp_type() != CPPTYPE_MESSAGE`), so it was red regardless of code correctness and left the `:419` MAP-struct-value site uncovered. Respecified onto a chain-independent `field:`-scope key on `Coord`'s own leaf.
**What landed:** #121's `OptionMetadataResolver` is threaded from `Generate()` (per-`Generate()` stack-local, built only `if (!rules.empty())` so `resolver == nullptr` literally means no-rules) into the GIR-5 IR schema visitor. `SchemaFieldRecord` accumulates the outer→inner `(fletcher.flatten_field)` wrapper chain alongside the `id_prefix` it already threaded (orientation is load-bearing: `ForField` scans with `rbegin()`). One pair vector per node (`RootMetadata`/`FieldMetadata`, builtins then resolver extras) feeds the single existing `SetMetadata` call site; `CppSchemaSink::SetMetadata` escapes **key and value**; **both** `EmitNodeType` deep-copy sites pass the resolver (`:404` STRUCT/LIST, `:419` MAP struct value — the second was missing from the first design draft). **This makes #121's own central invariant structural**: #121 kept `EmitMetadataBlock` and `SetMetadataPairs` consuming an identical pair vector *by hand*; after GIR there is one vector, one call site, two renderers.
**Also fixed — a live #121 defect this item made reachable:** `EscapeCppStringLiteral` emitted octal escapes that a following ASCII digit could terminate (`"\3022"`), which is MSVC **C4125** at `/W4` — so a consumer building the *generated header* with `/W4 /WX` would fail. At `HEAD` the escaper had **zero production callers**; GIR-13 §4 is what first puts its output into a consumer-compiled header, so the hazard becomes reachable in this item. Fixed by splitting the literal (`"\302" "2"`), byte-neutral (proven by `static_assert` on length + elements). Compliance reproduced C4125 with real `cl.exe` on the pre-fix literal and confirmed all **134** `ArrowCharView` literals in the real generated header now compile clean at `/W4 /WX`.
**Files touched:** `protoc/include/{cpp_backend_schema_visitor,schema_builder,option_metadata}.hpp`, `protoc/src/{cpp_backend_schema_visitor,generator,option_metadata}.cpp`, `protoc/tests/{CMakeLists.txt,test_schema_visitor.cpp,test_option_metadata.cpp}`, `integration-tests/protoc-arrow-bridge/{proto/option_metadata.proto,tests/test_metadata_options.cpp}`. RBA emitter untouched; `docs/fletcher-options.md` unedited (contract unchanged); `protoc/tests/golden/*.ipc` byte-identical.
**Reviews:** 4a compliance **CONFORMS** (0 blocking) — then re-run because the C4125 fix edited five files the design marked "no change"; re-review **CONFORMS**, holding that the "no change" row was a *porting* statement, not an invariant like the RBA/goldens/`fletcher-options.md` rows, and that `option_metadata.cpp` is inside locked #10's `protoc/` In-clause and is the only correct site. The `integration-tests/protoc-arrow-bridge` edit is explicitly **not** #10's admitted compile-and-run harness (that is `protoc-coverage`) but is a *generator test*, which #10 names; the fixture change is strictly strengthening. 4b code-review **0 blocking / 4 should-fix / 10 nits** → all 4 should-fix FIXED (C4125; octal branch never compiled in any lane; `ArrowRowGenerator::Generate` untested so an IPC-loop `nullptr` was invisible to all 88 unit tests; a comment asserting an ordering `ValidateNoUnsupportedIr` contradicts). **Both reviewers mutation-tested the guards rather than trusting them** — dropping the resolver at each deep-copy site, reversing the chain, stripping escaping, moving escaping out of the sink. T1/T2/T3 discriminate; **T4 does not, structurally** (it compares two traces from the same visitor, so a visitor-side mutation perturbs both identically) — T4 stays an ordering-only guard and T2 is the discriminator. (full: `plans/reviews/GIR-13-conformance.md`, `plans/reviews/GIR-13-codereview.md`)
**Verification:** protoc unit **92 pass / 0 fail / 1 gated skip** (+ test_package 3/3) · `integration-tests/protoc-arrow-bridge` **89 pass / 0 fail / 1 pre-existing rustc skip** · `integration-tests/protoc-coverage` **20/20** · `protoc/tests/golden/*.ipc` byte-identical · clang-format deviation counts identical to HEAD on every touched file (zero added). **Locked #2 discharged empirically, not by argument:** escaping-on vs escaping-off with no rules is byte-identical across all **76** generated artifacts of the arrow-bridge proto corpus.
**Commit / push:** `feature/generator-ir-rewrite` → `origin feature/generator-ir-rewrite`
**Carry-forwards / stop-and-asks:**
- **#121 is now fully reconciled onto the IR. The GIR round's merge blocker is cleared.** At the time of writing the branch was based on `hard/3-7-consolidated` (PR #124), not `main`, per the then-locked base. **[Updated 2026-08-27: #124 squash-merged as `499624d`; the branch is rebased onto `main` with the 9 HARD commits dropped, `hard/3-7-consolidated` is deleted, and the locked base is discharged. `main` is the base.]**
- **No lane compiles generated headers at `/W4 /WX`** (RR-2), so the C4125 regression guard is a source-scanning assertion; compliance verified it faithful against real `cl.exe`. A lane that actually compiles at `/W4 /WX` would be a stronger guard — candidate for a future round.
- Several scoping-note assumptions were **wrong** and shrank the item: #121's three integration TUs, `option_metadata.proto`, the `_META_RULES` block and the conanfile rules were already present and wired (the rebase brought them), so no porting was needed; and `test_metadata_nodrift` compares two live plugin runs, so there was no committed baseline to re-baseline. `test_option_metadata.cpp` holds **33** tests, not 32 — 30 untouched, 2 stale expectations corrected, 1 guard added.
- Design-doc rows still read "no change" for the five deviating files; see the conformance re-review for why that was overridden (RR-3).

## GIR-14 — PR #125 review response (2026-08-31)

**Trigger:** PR #125 review by `mortenbreiner` (`CHANGES_REQUESTED`, 12 findings) plus 2 Copilot inline comments (both already fixed in `75bee20`). All 12 findings were re-verified against `75bee20` before any code changed; all 12 hold substantively. Two needed calibration, recorded below.

**Findings 1, 2, 3 confirmed EMPIRICALLY, not by reading.** The reviewer flagged their own method caveat (traced by grep, no repro built). A repro corpus was built and run against a plugin compiled at `75bee20`:
- **#1** `message M { repeated L3 depth4 = 1; int32 sentinel = 2; }` (4 list levels) generated **exit 0, no warning**, with `row.reserve(2)` and only ONE `push_back` — the depth-4 field emitted a dead `{ auto coord_type = arrow::int32(); }` block. Schema struct had 2 children (4× `NANOARROW_TYPE_LIST` + `INT32`). Row storage, edge encode (4× `BeginList`) and edge decode (4× `resize`) were all correctly depth-generic; **only `ToArrowRow` and the view accessor broke.**
- **#1 sub-claim** the nullable form (`optional L4`) emitted an unbalanced `}` — generated header does not compile.
- **#2** the depth-4 column got `ArrowNestedScalarList<int32_t, arrow::Int32Array>` — the depth-**2** template.
- **#3** repeated form **hung** (killed at 20 s); singular form returned **`3221225725` = `0xC00000FD` = STATUS_STACK_OVERFLOW**.

**One root cause behind 1, 2 and 3, not three.** The classifier had no depth bound and no cycle guard on the flatten chain, while the RBA emitter already drew exactly that line (`IsSupportedNestedList`: depth 2 or 3, spec §6).

**What landed:**
- **1 + 2** — `kMaxNestedListDepth = 3` and a `ListChainDepth()` post-condition at the **single** `BuildFieldIr` entry point (dispatch split into `BuildFieldIrDispatch`). Because `BuildStructVariant` builds members via `BuildFieldIr`, one site covers every construction path, present and future. Over-deep fields become `UNSUPPORTED`, so GIR-8's `ValidateNoUnsupportedIr` fails the build with a clear message — the #55 contract — instead of emitting quietly-wrong code. Lifting the bound means making both emitters depth-generic, **not** raising the constant; that is said in the comment.
- **3** — `IsRecursive(msg)` now precedes `HasMessageFlatten(msg)` in both `BuildRepeatedMessage` and `BuildSingularMessage`. A flatten wrapper has exactly one field, so a cycle anywhere below it is a cycle the chain walk *would* follow; testing the hard unsupported condition first makes the walk unreachable for cyclic input. Precise, and no visited set needed.
- **4** — new `protoc/tests/test_schema_codec_lockstep.cpp`. `IsSchemaRepresentable` (schema + TS) and `ProjectIrToFieldMapping`-nullopt (everything else) were two hand-written walks answering one question, with a comment claiming "exact structural lockstep" — a MANUAL invariant, the drift class this round eliminated everywhere else. Now asserted: a corpus interleaving KEPT and DROPPED shapes (oneof, over-depth, recursive wrapper, both nested-list leaf kinds) asserts equal length and equal `name`/`field_id` per slot, plus a second test pinning that the corpus really exercises both outcomes.
- **6** — flatten-wrapper leaf error said "map value type unsupported" (copied from `BuildMapNode`, no map involved); now "flatten wrapper leaf type unsupported", matching the next branch.
- **7** — deleted 5 dead functions (**256 lines**): `EmitNanoarrowTypeSetup`, `CheckNa`, `SetScalarSchemaType`, `SetMetadataPairs`, `RequireNestedMsg`, plus the comment block still claiming "Any change to the emitted schema code must be mirrored here" — false since GIR-5 and pointing maintainers at code that never executes. `BuildMessageSchemaInto` (live, delegates to the IR visitor) retained.
- **8** — restored `PluginOptions` / `ParsePluginParameter`. **Worse than the review stated:** the refactor arrived with #121 (`b71b5c2`), the branch is a true fast-forward from `main`, and `git log -S` names **GIR-3 (`70fddc2`)** as the commit that deleted it — an active on-branch deletion, not a rebase casualty. Both `Generate()` and `GenerateAll()` now share the one tokenizer, so a new opt token reaches both.
- **10** — the map-key guard's `|| key == TYPE_ENUM` term was dead (`IsSupportedScalarType` returns true for `TYPE_ENUM`, and the inner `if` re-tested only the first term), and its comment claimed the opposite of the behaviour. Collapsed to the one condition it actually was.
- **11** — dropped `GenerateSchemaFunction`'s `fields` parameter (was `(void)fields;`) and updated the call site, so the signature no longer implies the schema is driven by `GatherFields` order.
- **12** — PR description said MSVC **C4834** ("discarding return value"); every in-repo reference correctly says **C4125** ("decimal digit terminates octal escape sequence"). Description corrected.

**Guards mutation-tested in both directions, per this round's practice:**
- `kMaxNestedListDepth = 3` → `99`: `Depth4ScalarLeafNestedListIsUnsupportedNotSilentlyWrong` and `CorpusActuallyExercisesBothKeptAndDroppedShapes` both go **RED**. `BothWalksKeepAndDropExactlyTheSameFields` stays green — correctly, since both walks consistently *keep* the field when the bound is lifted; the two tests discriminate different things.
- flatten-before-recursive ordering restored: `SelfReferentialFlattenWrapperIsUnsupportedAndTerminates` **times out** (it hangs exactly as the bug did).
- `protoc/tests/CMakeLists.txt` now sets `TIMEOUT 120` on the discovered tests: the cycle guard's regression signal is a hang, not an assertion, so without a cap a re-regression would stall CI for ctest's 25-minute default. Every test in the suite runs in under 0.1 s.

**Files touched:** `protoc/src/{ir,generator}.cpp`, `protoc/include/generator_internal.hpp`, `protoc/tests/{test_ir.cpp,test_schema_codec_lockstep.cpp,CMakeLists.txt}`. RBA emitter untouched; `docs/fletcher-options.md` unedited.

**Deliberately NOT fixed — each needs an owner decision, not a code edit:**
- **#5 (metadata values containing NUL are truncated).** Confirmed: nanoarrow's `ArrowCharView` is `strlen`-based, so both the in-process `key.c_str()` path and the generated-header literal truncate. **Already known and documented** — `test_option_metadata.cpp:118-121` says so verbatim, and the branch's stance is "the two paths still agree". The reviewer's added point stands: both paths lose data relative to the declared option, and byte-parity tests stay green while it happens. Fixing it means either rejecting NUL-bearing metadata in `ParseMetadataRules` (a user-facing contract change to #121, touching `docs/fletcher-options.md`, which this PR holds unedited) or emitting an explicit length. Pre-existing from #121. **Own ticket.**
- **#9 (`tsc` / `cargo` guards skip).** `tsc` half confirmed: neither CI job installs typescript (the Linux devcontainer image has `nodejs` + `prettier` only), so `CoverageHarness.GeneratedTypescriptCompiles` reports Skipped and the workflow stays green — the PR's claim of a harness that compiles "TS via `tsc`" is not enforced anywhere. **Rust half is overstated:** the Windows job runs on `windows-2022`, whose runner image ships Rust, so `cargo` may well be found there — worth reading a green run's log rather than accepting "never runs". Fixing needs a devcontainer image rebuild. **Own ticket.**
