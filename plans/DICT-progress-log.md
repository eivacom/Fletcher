# DICT — Progress Log

One section is appended per item by the runbook after it is green, reviewed,
logged, and pushed. See [DICT-dictionary-option.md](DICT-dictionary-option.md)
for the tracker.

<!-- Entries appended below by the round runbook -->

## DICT-1 — Option surface + reader (2026-08-28)

**Forcing test:** `TypeMapperTest.ReadsDictionaryOption` (ctest #37) → 🟢 (⚪ → 🔴 → 🟢). RED-FIRST evidence in two stages: (1) compile-red on the missing surface (`Option "(fletcher.dictionary)" unknown`), then (2) with the surface added and the reader stubbed, **behavioural** red — `HasFieldDictionary(f) Actual: false` / `d.has_value() Actual: false`.
**Design:** `plans/DICT-1-option-surface-reader.md` · Step-2: APPROVE at cycle 2 (8 items → 0; three residual nits fixed inline by the reviewer).
**What landed:** `(fletcher.dictionary)` extension **50001** on `FieldOptions` (+ `DictionaryIndexType`, `DictionaryOptions`) in `protoc/include/fletcher/options.proto`. New `option_reader` module holding the shared `ReparseOptionsWithPool` primitive plus typed `ReadFieldDictionaryOption` / `HasFieldDictionary`. `ir::FieldFacts` gains `dictionary_index_kind` + `dictionary_ordered`, populated in `BaseFacts` and propagated leaf-wins through `BuildFlattenedSingular` via the new `ApplyDictionaryFacts` (which recurses into a LIST's element so placement matches `BuildRepeatedScalarOrEnum`).
**Mechanism decision — locked #10's walker mandate RETIRES.** The plan mandated a hand-rolled unknown-field walker to avoid linking `options.pb.cc`. Instead this item **reuses PR #121's reflection reader**: serialize the linked-in `FieldOptions`, re-parse as a `DynamicMessage` from `field->file()->pool()` (which knows the extension). `ReparseOptionsWithPool` was extracted from `OptionMetadataResolver::Impl::ParsedOptions` as a shared primitive — **`test_option_metadata.cpp` has zero lines of diff** and all 33 of #121's tests stayed green, with the `cache.find` hit and the factory-before-cache member order (`option_metadata.cpp:243` before `:253`) preserved and now pinned by comment. The constraint in locked #10 (do not link `options.pb.cc`) still holds; only the mechanism changed. `index_type` decodes by **enum symbol name**, not number.
**Files touched:** `protoc/include/fletcher/options.proto`, `protoc/include/ir.hpp`, `protoc/src/ir.cpp`, `protoc/src/option_metadata.cpp`, `protoc/CMakeLists.txt`, `protoc/tests/{CMakeLists.txt,test_type_mapper.cpp}`, `docs/{dictionary-option-spec.md,fletcher-options.md}`; new `protoc/include/option_reader.hpp`, `protoc/src/option_reader.cpp`, `protoc/tests/proto_text_pool.hpp`.
**Reviews:** 4a CONFORMS → (fix round) **DEVIATES ×1** → CONFORMS. 4b 0 blocking / 4 should-fix / 6 P2 → (delta) 0 blocking / 0 should-fix, "close DICT-1". Both reviewers mutation-tested rather than trusting the suite. Full: `plans/reviews/DICT-1-conformance.md`, `plans/reviews/DICT-1-codereview.md`.
**Verification (PM-measured on the final tree, clean rebuild after purging the Conan cache):** **94 ctest tests, 0 failed, 1 skipped** (`SchemaVisitor.CaptureGoldens`, pre-existing gated golden-regen) — of which 93 are gtest cases (92 pass + that skip) and 1 is the pure-CMake `GenErrors.NoValueOrDieInEmitterSources` guard. test_package 3/3. Whole-tree clang-format 18.1.3 `--dry-run --Werror` exit 0.
**Carry-forwards / stop-and-asks:**
- **ACCEPTED DEVIATION — no warning path.** The design's D4 snippet warned on a wrapper/leaf dictionary conflict via `facts.warning`. **That channel is dead**: GIR-5 dropped per-field warning comments (`generator.cpp:816-824`), no emitter reads `FieldMapping.warning`, and `type_mapper.hpp:42`'s "emit as a comment" doc is stale. So `BuildFlattenedSingular` resolves the conflict **silently** and leaf-wins is asserted via index kind (wrapper INT64 vs leaf INT8 → INT8) instead. Both reviewers accepted this. **DICT-2 must not read D4's snippet as a live requirement** — escalation policy is DICT-2's call, and it needs a real channel first.
- **DICT-2 OWNS the `flatten_field` wrapper hole (design D4b).** Field-level `(fletcher.flatten_field)` is expanded at `cpp_backend_schema_visitor.cpp:66-109` (`BuildFlattenedFieldListImpl`) and `generator.cpp:598-610` (`GatherFieldsImpl`), both of which `continue` past the wrapper — so a **wrapper-declared** dictionary is silently ignored and never reaches DICT-2's kind gate, which spec §4 says must reject it. Requires a raw-descriptor walk. **This is the same walk that dropped wrapper context in GIR-13** — treat it as a known hazard of that code, not a one-off. `HasFieldFlatten` has exactly three call sites, all tabled in D4b; the third (`generator.cpp:129-149`) is confirmed NOT a drop site.
- **DICT-2 also owns:** the `BuildFlattenedRepeated` inner-declared drop (spec §7.1, deferred with pinning sub-cases — all seven return sites are LIST/UNSUPPORTED which DICT-2 rejects anyway), and **deleting the dead `ir::DictionaryModifier`** (N-14). That last one matters: it is a second dictionary carrier sitting one screen from the live one, and 4b flagged it as the leftover most likely to mislead.
- **The placement rule was overstated and is now a per-shape table.** RR-1 (blocking): the rule claimed "every IR node built from that field carries it". False for `repeated WrapRep xs [(fletcher.dictionary)]` where `WrapRep{flatten; repeated string values}` — outer LIST true, **intermediate LIST false**, leaf SCALAR true, so a consumer finds→loses→refinds the fact. Both reviewers found it independently (4b as P2, 4a as blocking); 4a's grading was right, and its sharp observation was that the suite's outer-declared case used depth 0, **the single depth at which the claim held**. Now a five-row table in `BaseFacts`' comment and spec §7.1, guarded by a sub-case asserting both directions. Non-blocking gap left: no row for non-flatten `repeated <message>`.
- **Process note.** The RR-1 fix was authored unstaged and 4a caught that the index still held the pre-fix text — committing it would have landed the authoritative spec still carrying the blocked invariant, with no guarding sub-case. Staging is the PM's job and it was missed. **Stage before dispatching any review, and re-stage after every fix round.** Related: a reviewer's `git checkout --` to revert its own mutation restored `ir.cpp` from the index and discarded the unstaged fix; it recovered from a copy and disclosed it, and the file was independently re-verified (brace-balanced, compiles, suite green).
**Commit / push:** `feature/dictionary-option` → `origin feature/dictionary-option`

## DICT-1.5 — Front-end guard: reject accessor/rust for dictionary fields (2026-08-28)

**Forcing tests:** `GenErrors.DictionaryRejectedBy_accessor` / `_rust` → 🟢 (⚪ → 🔴 → 🟢). RED-FIRST: before the guard the plugin exited 0 on `coverage_dictionary.proto` and emitted an accessor that would mis-read the column.
**Design:** `plans/DICT-1.5-backend-support-guard.md` · Step-2: APPROVE at cycle 2 (3 blocking + 2 required → 0).
**What landed:** a file-local `FindDictionaryField` predicate added **inside GIR-10's existing `ValidateBackendsSupportFields` field loop** (`protoc/src/generator.cpp`) — no second validation mechanism. It is **IR-based**, reads the live carrier `ir::FieldFacts.dictionary`, and descends **unconditionally** (an interior `dictionary == false` is not authoritative — spec §7.1's per-shape table). Error names the option, the field FQN, and round RIR. Runs front-end, after `ValidateNoUnsupportedIr` (#55) so a genuinely unsupported type still reports its own error first.
**Why IR-based and not descriptor-based:** schema emission derives dictionary-ness from the same IR, so the guard's inspected node set is a **superset** of the set emittable-as-dictionary — no false negatives, and a descriptor walk would only add false positives. Verified by the step-2 reviewer: `facts.dictionary` is written at only `ir.cpp:62` and `:295` repo-wide, and all twenty `BaseFacts(` sites appear in the design's closed-set table.
**The DICT-1 wrapper hole does NOT apply here** — that is a *projection*-level hole in `GatherFieldsImpl` / `BuildFlattenedFieldListImpl`, and this pass uses neither: it calls `ir::BuildFieldIr(msg->field(i))` directly, so `BaseFacts(w)` at `ir.cpp:545` reads a `flatten_field` wrapper's own option. Verified in the tree, not assumed. The one IR-invisible shape (spec §7.1 gap 2, `BuildFlattenedRepeated` inner-declared) is safe **by construction**: emission drops the fact identically, so there is no mis-read to guard against.
**Files touched:** `protoc/src/generator.cpp`, `protoc/include/type_mapper.hpp`, `docs/{dictionary-option-spec.md,fletcher-options.md}`, `integration-tests/protoc-coverage/{CMakeLists.txt,cmake/run_backend_guard_check.cmake}`; new fixtures `coverage_dictionary{,_wkt,_flatten,_field_flatten,_unsupported,_struct_child,_struct_child_inner,_list_child,_map_child}.proto`. **Zero diff** to `recordbatch_accessor_emitter.*`, Rust emission, `ir.*`, `option_reader.*`, `cpp_backend_*`.
**Test set (10 cases):** the two forcing tests, plus `_wkt` (locked #9 — `StringValue [(fletcher.dictionary)]` is a true positive), `_flatten`, `_fieldFlatten`, `_structChild`, `_listChild`, `_mapChild`, `DictionaryAcceptedWithoutRbaBackends` (no-false-positive + artifacts), and `DictionaryGuardDoesNotMaskUnsupportedType` (ordering vs #55).
**Reviews:** 4a **CONFORMS** (0 blocking; both implementer-flagged deviations accepted). 4b **APPROVE with 2 should-fix** → both discharged → **1 new should-fix (S3)** → fixed. Full: `plans/reviews/DICT-1.5-conformance.md`, `plans/reviews/DICT-1.5-codereview.md`.
**Verification (PM-measured independently on the final tree, after clearing `protoc-coverage/build`):** protoc unit **94 ctest / 0 failed / 1 pre-existing gated skip**; `protoc-coverage` full **30/30** (2 pre-existing `tsc` skips) with all ten dictionary guard tests passing; implementer additionally ran `-R GenErrors -j 8` **eight times — 128/128 executions, 0 failures**. clang-format 18.1.3 whole-tree clean.
**Carry-forwards / stop-and-asks:**
- **DICT-3 MUST keep dictionary-ness IR-derived.** DICT-1.5's safety for the message-level-flatten-wrapper inner-declared shape depends on schema emission reading the same `ir::FieldFacts.dictionary` node the guard reads. If DICT-3 introduces a second, descriptor-based source (e.g. calling `HasFieldDictionary` on the field directly), that shape silently becomes a **real, unguarded mis-read** and the superset property (design D1 / Risk 2) must be re-checked first. Recorded as a bullet under DICT-3's Scope.
- **`FIXED_SIZE_LIST` walk edge is disclosed, not fixtured** — `ir.cpp` never constructs one, so the branch is unconstructible from any real `.proto`. Accepted by 4b; mirrors `FindUnsupportedIr`'s own pre-existing gap.
- **DICT-2 still owns** the `flatten_field` projection hole (DICT-1 design D4b), the `BuildFlattenedRepeated` inner-declared drop (spec §7.1), and **deleting the dead `ir::DictionaryModifier`** — a second dictionary carrier one screen from the live one. This item did not read it (verified: all six diff mentions are comments warning against it).
- **RIR removes this guard** in the same change that adds accessor dictionary support (DICT locked #11) — a guard that outlives its subject is a false pass.
**Process notes — three review findings were "a check that does not check what it claims", the dominant defect class of this round and the GIR round before it:**
1. The first `coverage_dictionary_field_flatten.proto` did not exercise the wrapper path at all (`DictFfWrap` had no message-level flatten, so it was visited directly). Now the dictionary sits on the wrapper field `w` — the only spelling both emit walks `continue` past — so `BaseFacts(w)` is the sole route and the fixture is red-if-broken.
2. `EXPECT_SUCCESS` asserted `rc == 0` only, so it would have passed if the plugin emitted nothing. Fixed with `EXPECT_ARTIFACTS` (exists + non-zero size) — **and then that fix was itself vacuously passable**, because the shared `OUT_DIR` was never cleaned, so a stale artifact from a previous run satisfied it (4b demonstrated with a planted `nope.h`; 4a found it independently as M2). Fixed with `REMOVE_RECURSE` — **which then introduced a race** under `ctest -j` (S3, reproduced 1-in-8), fixed with per-test `OUT_DIR`. Three rounds on one assertion.
3. Deleting the LIST, FIXED_SIZE_LIST and MAP walk edges left all 8 tests green (S1). Now each edge has an isolating fixture; 4b verified by its own mutation runs that deleting LIST reds only `listChild` and deleting MAP reds only `mapChild`.
**Process note — delivery mechanics, twice, both PM error:** DICT-1's blocking fix was authored unstaged and the index still held the pre-fix text (caught by 4a); DICT-1.5's two new fixtures were left **untracked**, so tests passed locally off files a fresh clone and CI's `git ls-files` scan would not have had (caught by 4b). **Explicit gate needed, not a habit: stage before dispatching any review, re-stage after every fix round, and check `git status` for untracked files before committing.**
**Commit / push:** `feature/dictionary-option` → `origin feature/dictionary-option`

## DICT-2 — Mapper wiring + validation (2026-08-28)

**Forcing test:** `TypeMapperTest.DictionaryMappingAndRejections` → 🟢 (⚪ → 🔴 → 🟢). Red-first in two stages: compile-red (`is_dictionary` / `dict_index_type_expr` / `DictionaryUnsupportedReason` not found), then behavioural-red with stubs — 15 illegal shapes each reporting "was accepted, expected a rejection", with no fixture/path failures.
**Design:** `plans/DICT-2-mapper-wiring-validation.md` (rev 2 + step-4 addendum) · Step-2: APPROVE at cycle 2 (5 blocking + 3 required → 0, with three residual items applied inline by the reviewer).
**What landed:** the positive projection (`FieldMapping.is_dictionary` + `dict_index_type_expr`, derived from `ir::FieldFacts.dictionary`; value type untouched) plus **one fatal front-end pass** `ValidateDictionaryDeclarations` (`generator.cpp`) over five rules in `type_mapper.cpp` (`DictionaryUnsupportedReason` / `FindIllegalDictionaryField`): R1 `flatten_field` wrapper · R2 kind gate on the top-level `NodeKind::SCALAR` · R3 `ordered: true` (locked #8) · R4 conflicting declarations along a flatten chain · R5 repeated-flatten inner-declared. Dead `ir::DictionaryModifier` deleted. Runs **before** DICT-1.5's guard (an illegal declaration is the root cause; DICT-1.5's "regenerate without accessor,rust" would be misleading advice), so `GenErrors.DictionaryRejectedBy_accessor_fieldFlatten` was deliberately retargeted with a `_passOrder` sibling.
**Why a fatal pass and not `MapField -> nullopt`:** nullopt is a *silently dropped column*, and it would drift `ProjectIrToFieldMapping` against `IsSchemaRepresentable`. Locked #9's "a clear `UnsupportedReason`" was clarified this round to mean a reason **string**, not the function — that function's sole consumer is `generator.cpp:619`, a comment with exit 0, so the literal reading would make "a codegen error" not an error.
**S3 CLOSED — the walk now descends.** `FindIllegalDictionaryFieldImpl` descends into a singular message child, a **list element**, and a map value, never into an `IsFlattenedWrapper` or `IsRecursive`. Motivation: `protoc inner2.proto` rejected `xf.i2.M.p` while `protoc outer2.proto` accepted the *same declaration* — a verdict that depended on which file you compiled.
**Three holes remain, all disclosed in spec §7.1.1 with reproductions:** (a) **S2** — `map<string,W>` where `W` is a flatten wrapper whose inner field declares `ordered: true` exits 0; unit-pinned via `HostMapWrapper` (which must stay legal). (b) **P1-C** — a struct leaf reached *through* a `(fletcher.flatten)` wrapper hop is deep-copied but excluded by `!IsFlattenedWrapper(child)`, so the file-choice inconsistency survives one hop (`leaf_nest.proto` / `leaf_sing.proto` rc 0 while `leaf_inner.proto` rc 1). (c) gap-2's sibling, keeping its step-2 resolution (do **not** generalise R5). **None is fixable by descending into the wrapper** — that reintroduces the R1 false positive the new ctest now guards. The correct shape is to follow the wrapper chain to its leaf and judge the leaf; strictly smaller than gating R1, and it can land first.
**Files touched:** `protoc/{include,src}/type_mapper.{hpp,cpp}`, `protoc/include/cpp_backend_type_table.hpp` + `.cpp`, `protoc/src/generator.cpp`, `protoc/include/ir.hpp` (dead enum removed), `protoc/tests/test_type_mapper.cpp`, `integration-tests/protoc-coverage/{CMakeLists.txt,proto/*}`, `docs/{dictionary-option-spec.md,fletcher-options.md}`, the story plan. **Zero diff** to `recordbatch_accessor_emitter.*`, Rust emission, the option surface, the reader, `ir.cpp`, `cpp_backend_schema_visitor.*`, runtimes.
**Reviews:** 4a **CONFORMS** → (fix round) **CONFORMS** — verified via build+run, three source mutations, six probe protos and a **58-artifact byte-diff against a HEAD-built plugin**. 4b 0 blocking / 4 should-fix → two re-reviews, ending with the list-element extension confirmed safe. Full: `plans/reviews/DICT-2-{conformance,codereview}.md`.
**Verification (PM-gated):** protoc unit **95/95** + test_package 3/3; protoc-coverage clean rebuild **34/34** (2 pre-existing environmental skips); corpus sweep 19 protos — 12 accepted / **7 rejected, all intended**; clang-format 18.1.3 whole-tree clean.
**Carry-forwards / stop-and-asks:**
- **DICT-3 MUST gate on `facts.dictionary` AND `kind == SCALAR`.** Stated in four places. A non-SCALAR node *can* carry the fact into emission — proven live: `map<string,W>` over `W{flatten; repeated string vals [(dictionary)={INT8}]}` is rc 0 under `ipc` while `accessor` names `xf.ns.W.vals`, i.e. the fact sits on a **LIST**. Without the kind half, DICT-3 emits `dictionary(idx, <list>)`.
- **The three disclosed holes stop being harmless at DICT-3.** Today a swallowed declaration yields a **byte-identical artifact** (nothing reads `facts.dictionary` yet, and `dictionary_ordered` is read only by R3) — the cost is a missing diagnostic. Once DICT-3 emits dictionary encoding the same silence becomes a **silently wrong column type**. The chain-following follow-up should land **before or with DICT-3**.
- The story's "TWO schema emitters" DICT-3 bullet is marked superseded — GIR-5 unified them; `EmitNanoarrowTypeSetup`, `SetScalarSchemaType`, `ArrowTypeExpr` et al. are definition-only dead code post-GIR-5.
- `FieldMapping.dict_index_type_expr` has no production reader by design — it is spec §5.1's spelling for RIR's accessor type gate. Recorded in `plans/RIR-rba-onto-ir.md`: RIR should read `ir::FieldFacts.dictionary_index_kind` directly and retire both fields with `FieldKind`.
- Nit 10 carried with its trap documented: `facts.proto_full_name` is the *resolved chain's* field, not the declaring field when `ApplyDictionaryFacts` propagated it — a naive "name the declaring field" fix names a field carrying no declaration. P2-1's `BuildFieldIr` gate re-checked post-descent and still skipped; it is exact only while `ApplyDictionaryFacts` is the sole propagation route.
**Process notes:**
- **Three guards this round did not guard what they claimed.** (i) `Ctl.ord_wrap` was meant to prove `ordered` is compared, but differed from its leaf in *both* members, so an `index_kind`-only comparison passed it — found while the reviewer was verifying a fix for this very class. (ii) `Chains.ff_inside_wrapper` was added to stop the R1 false positive being reintroduced, but the unit tests never call `ValidateDictionaryDeclarations`, so it was inert; replaced with a plugin-level ctest that reds under exactly that mutation while the unit suite stays green. (iii) R4's two mandated `is_repeated` terms were present and correct but unpinned — deleting either left the suite green.
- **The reviewers reconciled, they did not conflict.** 4a proved that *dropping* the `IsFlattenedWrapper` skip rejects a live dictionary; 4b's closure *keeps* the skip and adds descent. Reading either in isolation would have produced the wrong decision.
- **An invariant over-claimed.** "The walk judges a child message exactly where emission deep-copies that child's schema" implied no hole remained; it also miscounted (two call sites, four emission positions — the LIST case recurses). Restated in terms of positions, with what is *not* judged named explicitly. The implementer also declined a PM suggestion to assert that gap-2's sibling shares P1-C's fix shape, on the grounds that asserting it unverified would be the same failure mode — correctly.
- **PM delivery-mechanics errors, third and fourth of the round:** the docs commit swallowed DICT-2's two fixtures because they were already staged and `git commit` takes the whole index; and unstaged `// MUTATION` markers were live in the source during a review dispatch. **Standing gate now:** stage before dispatching review, re-stage after each fix round, commit with an explicit pathspec, and check `git status --porcelain` + `git grep MUTATION -- protoc/` before every commit.
**Commit / push:** `feature/dictionary-option` -> `origin feature/dictionary-option`

## ROUND HALTED after DICT-2 — awaiting reassessment (2026-08-28)

**Status: DICT-1, DICT-1.5, DICT-2 are 🟢 and pushed. DICT-3, DICT-4, DICT-5 are ⚪ and NOT started.** DICT-3's design was authored and is committed for the record, but **nothing from it was implemented**. Halted by maintainer decision at the DICT-3 stop-and-ask below.

### Why the round stopped

DICT-3's design surfaced a blocker the round's premise did not account for:

> **nanoarrow's IPC writer rejects dictionary types outright.**
> `third_party/nanoarrow/nanoarrow_ipc.c`:
> `case NANOARROW_TYPE_DICTIONARY: ArrowErrorSet(error, "IPC encoding of dictionary types unsupported"); return ENOTSUP;`

Consequence: `SerializeSchemaIpc` throws for a dictionary schema, so **`CreateTopic` cannot announce one over any IPC-based provider** — FastDDS, XRCE-DDS, or the gateway. In-process and mock providers are unaffected.

So the feature as specified would generate a correct dictionary schema that **no real deployment could publish**. DICT-4's forcing test (`RoundtripRefoldsToDictionaryArray`, "via batched Subscribe") runs against a mock, so it would have gone green while the path stayed unusable in production — a green test for an unusable feature.

**This was already known in the tree, not a new discovery.** `pubsub/src/publisher.cpp:44-54` carries: *"Some valid schemas cannot be IPC-encoded (e.g. dictionary types, which nanoarrow's IPC writer rejects); if either side fails to serialize we cannot prove a conflict, so accept the re-declaration rather than throwing."* Someone hit this in the conflict-detection path and worked around it locally, without the consequence for a dictionary feature being drawn out.

There are **two** vendored nanoarrow copies (`protoc/third_party/`, `pubsub/third_party/`), so a fix means patching both.

### Options put to the maintainer (chose: stop and reassess)

1. Document as a v1 limitation and continue — ship the generator work, dictionary columns in-process/mock only.
2. Patch both vendored nanoarrow copies to implement Arrow IPC dictionary encoding (DictionaryEncoding metadata + dictionary batches). Substantial, third-party, needs its own round.
3. Land DICT-3 only, defer DICT-4/5.
4. **Stop now and reassess** ← chosen.

The reassessment question is essentially: *was `(fletcher.dictionary)` wanted for pub/sub?* If yes, the part that cannot work is exactly the part that was wanted, and the round's value assumption needs revisiting before more is built. If it was wanted for in-process Arrow consumers, option 1 remains viable.

### The tree is in a coherent state — stopping here is safe

The three holes disclosed in spec §7.1.1 are **harmless precisely because DICT-3 did not land**: nothing reads `facts.dictionary`, so a swallowed declaration produces a **byte-identical artifact** and the only cost is a missing diagnostic. That property holds as long as no emission gate exists. It would have flipped to *silently wrong column type* the moment DICT-3 emitted encoding — which is why the chain-following follow-up was scheduled to land before or with DICT-3.

What DICT-1/1.5/2 deliver standalone, with no dependency on DICT-3:
- the `(fletcher.dictionary)` option surface (extension 50001) and a reader,
- validation of every illegal declaration shape (five rules, fatal), and
- a guard refusing `--fletcher_opt=accessor` / `rust` rather than emitting an accessor that mis-reads.

Nothing emits dictionary encoding, so no user-visible schema changed.

### DICT-3 design findings worth keeping regardless of the decision

Recorded here because they outlive the round's fate:

- **`ArrowTypeName` lacks `INT8` / `INT16`.** A non-int32 dictionary index would render as `NANOARROW_TYPE_UNINITIALIZED` **in the generated C++ source only**. Any future DICT-3 forcing test must cover a non-int32 index — an int32-only test would pass over this.
- **DICT-1.5's documentation is now factually wrong** where it says the IPC schema output accepts dictionary fields. It does not. If DICT-3 is ever resumed, `--fletcher_opt=ipc` must become a front-end rejection.
- **The story's DICT-3 wiring instruction would break the coverage harness**: adding `dictionary` to `PROTO_STEMS` gives every stem `ipc`, which now fails. A dedicated custom command is needed instead.
- **The design's own timing analysis concluded the chain-following follow-up was NOT a prerequisite** — per hole: the repeated shape puts the fact on a LIST so the kind gate makes it byte-identical `list<utf8>`; gap-2's sibling never enters the IR; the singular and wrapper-hop shapes sit on genuine scalars where `dictionary(idx, value)` is the *correct* type, with `ordered` the only unmet declaration and v1 honouring it nowhere. That analysis stands if the round resumes.
- The branch point for a resumption is `832bff4`, and the DICT-3 design doc is `plans/DICT-3-schema-emission.md`.
