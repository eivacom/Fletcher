# DICT — Progress Log

One section is appended per item by the runbook after it is green, reviewed,
logged, and pushed. See [DICT-dictionary-option.md](DICT-dictionary-option.md)
for the tracker.

<!-- Entries appended below by the round runbook -->

## DICT-1 — Option surface + reader (2026-08-28)

**Forcing test:** `TypeMapperTest.ReadsDictionaryOption` (ctest #37) → 🟢 (⚪ → 🔴 → 🟢). RED-FIRST evidence in two stages: (1) compile-red on the missing surface (`Option "(fletcher.dictionary)" unknown`), then (2) with the surface added and the reader stubbed, **behavioural** red — `HasFieldDictionary(f) Actual: false` / `d.has_value() Actual: false`.
**Design:** `plans/DICT-1-option-surface-reader.md` · Step-2: APPROVE at cycle 2 (8 items → 0; three residual nits fixed inline by the reviewer).
**What landed:** `(fletcher.dictionary)` extension **50001** on `FieldOptions` (+ `DictionaryIndexType`, `DictionaryOptions`) in `protoc/include/fletcher/options.proto`. New `option_reader` module holding the shared `ReparseOptionsWithPool` primitive plus typed `ReadFieldDictionaryOption` / `HasFieldDictionary`. `ir::FieldFacts` gains `dictionary_index_kind` + `dictionary_ordered`, populated in `BaseFacts` and propagated leaf-wins through `BuildFlattenedSingular` via the new `ApplyDictionaryFacts` (which recurses into a LIST's element so placement matches `BuildRepeatedScalarOrEnum`).
**Mechanism decision — locked #10's walker mandate RETIRES.** The plan mandated a hand-rolled unknown-field walker to avoid linking `options.pb.cc`. Instead this item **reuses PR #121's reflection reader**: serialize the linked-in `FieldOptions`, re-parse as a `DynamicMessage` from `field->file()->pool()` (which knows the extension). `ReparseOptionsWithPool` was extracted from `OptionMetadataResolver::Impl::ParsedOptions` as a shared primitive — **`test_option_metadata.cpp` has zero lines of diff** and all 33 of #121's tests stayed green, with the `cache.find` hit and the factory-before-cache member order (`option_metadata.cpp:243` before `:253`) preserved and now pinned by comment. The constraint in locked #10 (do not link `options.pb.cc`) still holds; only the mechanism changed. `index_type` decodes by **enum symbol name**, not number.
**Files touched:** `protoc/include/fletcher/options.proto`, `protoc/include/ir.hpp`, `protoc/src/ir.cpp`, `protoc/src/option_metadata.cpp`, `protoc/CMakeLists.txt`, `protoc/tests/{CMakeLists.txt,test_type_mapper.cpp}`, `docs/{dictionary-option-spec.md,fletcher-options.md}`; new `protoc/include/option_reader.hpp`, `protoc/src/option_reader.cpp`, `protoc/tests/proto_text_pool.hpp`.
**Reviews:** 4a CONFORMS → (fix round) **DEVIATES ×1** → CONFORMS. 4b 0 blocking / 4 should-fix / 6 P2 → (delta) 0 blocking / 0 should-fix, "close DICT-1". Both reviewers mutation-tested rather than trusting the suite. Full: `plans/reviews/DICT-1-conformance.md`, `plans/reviews/DICT-1-codereview.md`.
**Verification (PM-measured on the final tree, clean rebuild after purging the Conan cache):** **94 ctest tests, 0 failed, 1 skipped** (`SchemaVisitor.CaptureGoldens`, pre-existing gated golden-regen) — of which 93 are gtest cases (92 pass + that skip) and 1 is the pure-CMake `GenErrors.NoValueOrDieInEmitterSources` guard. test_package 3/3. Whole-tree clang-format 18.1.3 `--dry-run --Werror` exit 0.
**Carry-forwards / stop-and-asks:**
- **ACCEPTED DEVIATION — no warning path.** The design's D4 snippet warned on a wrapper/leaf dictionary conflict via `facts.warning`. **That channel is dead**: GIR-5 dropped per-field warning comments (`generator.cpp:816-824`), no emitter reads `FieldMapping.warning`, and `type_mapper.hpp:42`'s "emit as a comment" doc is stale. So `BuildFlattenedSingular` resolves the conflict **silently** and leaf-wins is asserted via index kind (wrapper INT64 vs leaf INT8 → INT8) instead. Both reviewers accepted this. **DICT-2 must not read D4's snippet as a live requirement** — escalation policy is DICT-2's call, and it needs a real channel first.
- **DICT-2 OWNS the `flatten_field` wrapper hole (design D4b).** Field-level `(fletcher.flatten_field)` is expanded at `cpp_backend_schema_visitor.cpp:66-109` (`BuildFlattenedFieldListImpl`) and `generator.cpp:598-610` (`GatherFieldsImpl`), both of which `continue` past the wrapper — so a **wrapper-declared** dictionary is silently ignored and never reaches DICT-2's kind gate, which spec §4 says must reject it. Requires a raw-descriptor walk. **This is the same walk that dropped wrapper context in GIR-13** — treat it as a known hazard of that code, not a one-off. `HasFieldFlatten` has exactly three call sites, all tabled in D4b; the third (`generator.cpp:129-149`) is confirmed NOT a drop site.
- **DICT-2 also owns:** the `BuildFlattenedRepeated` inner-declared drop (spec §7.1, deferred with pinning sub-cases — all seven return sites are LIST/UNSUPPORTED which DICT-2 rejects anyway), and **deleting the dead `ir::DictionaryModifier`** (N-14). That last one matters: it is a second dictionary carrier sitting one screen from the live one, and 4b flagged it as the leftover most likely to mislead.
- **The placement rule was overstated and is now a per-shape table.** RR-1 (blocking): the rule claimed "every IR node built from that field carries it". False for `repeated WrapRep xs [(fletcher.dictionary)]` where `WrapRep{flatten; repeated string values}` — outer LIST true, **intermediate LIST false**, leaf SCALAR true, so a consumer finds→loses→refinds the fact. Both reviewers found it independently (4b as P2, 4a as blocking); 4a's grading was right, and its sharp observation was that the suite's outer-declared case used depth 0, **the single depth at which the claim held**. Now a five-row table in `BaseFacts`' comment and spec §7.1, guarded by a sub-case asserting both directions. Non-blocking gap left: no row for non-flatten `repeated <message>`.
- **Process note.** The RR-1 fix was authored unstaged and 4a caught that the index still held the pre-fix text — committing it would have landed the authoritative spec still carrying the blocked invariant, with no guarding sub-case. Staging is the PM's job and it was missed. **Stage before dispatching any review, and re-stage after every fix round.** Related: a reviewer's `git checkout --` to revert its own mutation restored `ir.cpp` from the index and discarded the unstaged fix; it recovered from a copy and disclosed it, and the file was independently re-verified (brace-balanced, compiles, suite green).
**Commit / push:** `feature/dictionary-option` → `origin feature/dictionary-option`
