{
  "name": "@eiva/fletcher-coverage-ts-check",
  "version": "0.0.0",
  "private": true,
  "description": "GIR-1 TypeScript compile-check for generated coverage.fletcher.ts. The CTest wrapper (cmake/run_tsc_check.cmake) uses whatever tsc is already on PATH; no network install is performed. These devDependencies document the toolchain a CI job would install to make the check active.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
