{
  "name": "protoc-gen-fletcher-npm-integration-test",
  "private": true,
  "version": "0.0.0",
  "description": "Consumer-perspective integration test for @eiva/protoc-gen-fletcher. Installs the npm shim from the local working copy, generates TypeScript bindings via the prebuild hook, compiles, and runs the resulting binary.",
  "license": "LGPL-3.0-or-later",
  "scripts": {
    "proto:gen": "protoc --plugin=protoc-gen-fletcher=./node_modules/.bin/protoc-gen-fletcher --fletcher_opt=ts --fletcher_out=src/generated -I proto proto/example.proto",
    "prebuild": "npm run proto:gen",
    "build": "tsc",
    "test": "node dist/main.js"
  },
  "devDependencies": {
    "@protobuf-ts/protoc": "^2.9.5",
    "typescript": "^5.4.0"
  }
}
