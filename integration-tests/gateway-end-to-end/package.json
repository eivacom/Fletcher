{
  "name": "gateway-end-to-end-integration",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end integration test: gateway (C++ WebSocket server) + gateway-client-ts (TypeScript client) over a real WebSocket.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
